# SYSTEM_MAP — tokyo-trip

> 純靜態 PWA，無後端伺服器。本圖描述檔案結構與依賴關係，SA 影響分析以此為準。
> 狀態標記：✅ 已存在（草稿，可採用/重寫）｜🔨 進行中｜⬜ 規劃中

## 檔案結構

```
tokyo-trip/
├── index.html              ✅ Task1–3,8,5,12,14,6,17  App shell：iOS meta、分頁容器、載入所有 js（Task2 加 tts/bigtext/phrases-tab、Task3 加 import-data/trip-tab、Task5 加 api/translate-tab（插 trip-tab.js 後、coupon-viewer.js 前）、Task12 加 recorder.js（插 api.js 後、translate-tab.js 前，最終 api → recorder → translate-tab）、Task6 加 camera-tab.js（插 translate-tab.js 後、coupon-viewer.js 前——wrap 鏈四層順序依此定死），順序定死；Task8 狀態列 black-translucent→default、theme-color→#F5F6F8；Task14 加 version.js 於**所有 script 最前**（A6 注解改寫：version → config → phrases → tripdata → app → 功能模組）＋兩 DOM 元素 #app-version／button#update-toast[hidden]（section 外掛 body，HTML 不寫死版號字串——文字由 app.js 填）；Task6 `#tab-camera` 佔位卡清除、section 留空（DOM 由 camera-tab.js 動態建構）；Task17 加第 6 顆導覽鈕 map（🗺，trip 與 coupons 之間，與 TAB_IDS 順序對齊）＋`<section id="tab-map" hidden>`（tab-trip/tab-coupons 之間）＋兩支 script（mapdata.js 插 tripdata.js 後資料層、map-tab.js 插 trip-tab.js 後功能層），並**清除全部 4 處 Task1 殘留 placeholder-card**（phrases/translate/trip/coupons，各 section 留空由 JS 建構））
├── css/
│   └── style.css           ✅ Task1–3,8,11,10,5,12,14,15,6,17  全站樣式（直式 iPhone、safe-area；Task2 加常用句/大字 overlay；Task3 加 trip 分頁 8 視覺區塊：pill 導覽/日卡/航班卡/飯店卡/重要資料/私人段/匯入匯出區/暫時提示；Task8 淺色主題：:root 變數翻轉＋新增 --c-accent-text、硬編碼破口 A1–A10 全清、overlay 深底解耦、.phrases-chips-* 分類 chips 樣式；Task11 導覽列淺色化＋放大（#nav-bar 白底、--nav-h 60px、icon 28px/label 12px）、航班/飯店卡淺色化（A9 深底 override 整塊刪除）、.phrases-chips-bar 加 flex-shrink:0；Task10 :root 五階 type scale 變數（--fs-xl/lg/md/sm/xs=22/19/17/15/13px，僅 .trip-* 授權）＋.trip-* 字級全數變數化（F1 三處 override：hotel-name/important-tel/flight-no 維持 xl=22）＋總覽層（.trip-itin-overview/.trip-ov-card）與單日層（.trip-itin-day/.trip-day-nav-*）版面＋.trip-item 重構為 grid（54px 1fr）＋孤兒樣式退場（.trip-day-card/header/label/date/theme/chevron/body、.trip-item-header/chevron/detail[hidden] 全刪）；Task5 translate 分頁樣式節（.translate-*：textarea 17px≥16px iOS 紅線、動作鈕≥44px、input-area flex-shrink:0、字級全硬編碼不用 --fs-*、既有選擇器零 diff）；Task12 對話語音模式樣式節（.translate-mode-seg segmented control 44px、.talk-container flex:1+min-height:0+overflow:hidden、.talk-history 唯一捲動區、.talk-bubble 氣泡（zh2ja accent 右/ja2zh 白底左、trans 22px hardcode）、氣泡動作鈕 44px、錄音中雙動畫（blink+ripple 紅 #C63A3A）、.talk-mic-* 60px 麥克風鈕、字級全硬編碼禁 --fs-*、無新增 z-index≥100）；Task14 版號徽章＋更新 toast 樣式節（#app-version：fixed bottom:var(--nav-h) 右下、11px 硬編碼、pointer-events:none、z-index:20；#update-toast：fixed 全寬橫幅、min-height:44px、accent 底白字、z-index:30；S6 防呆 `#update-toast[hidden]{display:none}` 防 display:flex 蓋掉 UA hidden；z-index 落 11–99 帶，overlay 100+ 蓋過屬預期）；Task15 對話面對面版面樣式節（Task12 氣泡架構 11 組孤兒選擇器全退場（.talk-lang-bar/.talk-history/.talk-bubble* /.talk-status*/.talk-mic-row/.talk-mic-zh/.talk-mic-ja 等），保留重用 .talk-container＋@keyframes talk-recording-blink/talk-mic-ripple＋.talk-mic-active class 名；新增 .talk-side flex:1 均分兩半、.talk-side-ja `transform:rotate(180deg)` 一處旋轉整容器、.talk-divider 中央 32px 分隔帶、.talk-side-result 每側唯一捲動區 28px hardcode≥28px 隔桌可讀、.talk-side-speak/.talk-side-bigtext 44px＋disabled opacity:0.30 非隱藏、.talk-side-mic 60px；容器內禁 fixed（旋轉 containing block 紀律）、字級全硬編碼禁 --fs-*）；Task6 camera 分頁樣式節（.camera-* 前綴、字級全硬編碼禁 --fs-*、無新增 z-index≥100：.camera-container flex column、.camera-lang-bar 靜態語言列 --c-accent-text、取景區 .camera-viewfinder-area 深底硬編碼 #0d0d0d（Task8 解耦紀律）＋.camera-video object-fit:cover＋.camera-scan-frame 掃描框四角 gradient 硬編碼 #4D7CF4＋.camera-scan-hint 白字提示、.camera-fallback-holder 降級佔位 #1a1a1a＋📷 圖示、.camera-processing-area spinner（@keyframes camera-spin）、結果卡 .camera-result-*（orig 16px/trans 22px 700、error 深紅 #C63A3A）、動作鈕 .camera-action-btn 基底 44px、.camera-bottom-bar flex-shrink:0（Task11 U2 紀律）＋.camera-album-btn 60px＋.camera-shutter-btn 72px≥64px 圓形快門）；Task17 map 分頁樣式節（.map-* 前綴、字級全硬編碼禁 --fs-*、無 z-index≥100、淺色主題：#tab-map overflow:hidden 覆蓋讓 .map-places-list 成唯一捲動區（flex:1/min-height:0/overflow-y:auto）、.map-chips-bar/.map-ab-row/.map-lang-hint 皆 flex-shrink:0（U2 紀律）、chip/place-btn/swap/route 鈕觸控目標全 ≥44px、.map-custom-input 16px iOS 縮放紅線、route 主鈕 48px accent 填充），並**刪除 Task1 殘留 4 個 placeholder 孤兒選擇器**（.placeholder-card/icon/title/desc，grep=0））
├── js/
│   ├── app.js              ✅ Task1,14,17  分頁切換框架、SW 註冊、共用工具（Task2 未改本體，showTab 被 bigtext.js 外掛 wrap；Task14 SW 註冊段擴充落地：`updateViaCache:'none'`、`hadController` register 前快照、visibilitychange(visible)/pageshow → `_triggerUpdate()`（S2 null-check＋S1 promise .catch）、controllerchange＋updatefound→activated 雙路 → `#update-toast`（`_updateShown` 記憶體 flag、首裝不彈）、toast click＝全 repo 唯一 `location.reload`、DOMContentLoaded 填 #app-version（缺元素/缺常數 no-op）；Task17 TAB_IDS additive 加 'map' 成六分頁 `['phrases','translate','camera','trip','map','coupons']`——既有五 id 原樣、registerTab/showTab 本體零 diff（indexOf/forEach 自動支援第六 id）；lastTab 合法值域隨之擴充含 'map'）
│   ├── version.js          ✅ Task14,15,6,16,17  版號單一來源（window.APP_VERSION／APP_VERSION_DATE 兩常數，現況 'v17'／'07/12'（Task17 bump）；APP_VERSION 與 sw.js CACHE_VERSION 逐字元相等＝QA 機械閘；載入順序全站最前；純常數零依賴，缺載 no-op 不壞頁）
│   ├── config.example.js   ✅        金鑰模板（上 git）
│   ├── config.js           ✅ Task5  真實金鑰（Task5 起納入版控、隨站公開部署；受 referer＋API 限制保護；嚴禁個資；載入仍須容錯）
│   ├── phrases.js          ✅ Task8,9  常用句庫（Task2 消費；Task8 每分類加 `id` 欄位—greetings/dining/shopping/transport/hotel/emergency；Task9 內容整理：移除 11 句國民基本用語＋新增 9 句實用句，現況 39 句（4/9/9/6/5/6），greetings 顯示名 cat 改「溝通・語言」（id 不變））
│   ├── tripdata.js         ✅ Task3–4  行程/航班/飯店/重要資料/折價券資料（單檔雙契約：TRIP=Task3 已換真實資料（schema 見 Task3.api.md，含 isoDate、無 members）、COUPONS=Task4 已換 16 筆真實券資料（schema 見 Task4.api.md：id/store/category/discount/expiry/passport/notes/area/img）；檔頭有隱私警告）
│   ├── mapdata.js          ✅ Task17  地圖地點資料（生成檔——由 `make_mapdata.py` 從 repo 外 KML 生成，**勿手改**；`window.MAPDATA` 依 isoDate 升冪 5 天共 49 點（0721:5/0722:12/0723:7/0724:17 築地在前＋橫濱合併/0725:8），每點 `{name,lat,lon}`（KML lon,lat 已對調）；公開地點座標無個資、入版控合法；無本機路徑/版號字串；行程改期須重跑腳本重生成——見人工補充區 MAPDATA↔TRIP 契約）
│   ├── tts.js              ✅ Task2,12,16  語音共用模組（`App.speak(text, lang?)`——Task12 加選填第二參預設 'ja-JP'，既有單參呼叫零變更；`_pickVoice(lang)` 一般化：正規化後精確比對→前綴→null（iOS zh-CN 陷阱修）；16ms pending timer 疊音縫修復（speak/cancel 開頭皆 clearTimeout）；Task16 加 `App.speak.unlock()`（iOS 手勢解鎖：空白字元 utterance 直呼 speechSynthesis.speak、不設 voice/lang、冪等旗標 `_unlocked` 發出即設、獨立參照 `_unlockUtterance` 防 GC、不碰 `_currentUtterance`/`_pendingTimer`、無 speechSynthesis no-op；additive——speak/cancelSpeak/_pickVoice/_updateVoices 四函式本體零 diff）；Task5 重用）
│   ├── bigtext.js          ✅ Task2,12  大字展示共用元件（`App.showBigText`——Task12 加選填 `lang` 欄（additive），`_lang` 每次呼叫必重設防殘留，播音鈕帶 `_lang`（預設 ja-JP 行為不變）；Task5/6 重用）
│   ├── recorder.js         ✅ Task12,16  錄音/編碼封裝（`App.recorder = { isAvailable, isRecording, start, stop()→Promise<{base64,durationMs}>, abort, ErrorCode }`；LINEAR16@16k、R1–R8 iOS 怪癖全內收（destination 必接/每次新建 AudioContext/webkit fallback+resume/資源釋放四件套/base64 分塊 0x8000）；ErrorCode 5 碼 MIC_DENIED/MIC_UNAVAILABLE/NO_AUDIO/NOT_SUPPORTED/OTHER；Task16 `_release()` Promise 化（audioCtx.close() 的 Promise 內部吞 rejection、無 audioCtx 回 Promise.resolve()）＋`stop()` resolve 語意改「音訊 session 釋放完成後」（尾端 `Promise.race([releaseP→result, 500ms timeout→result])`，close 懸掛不卡流程不丟結果）、`abort()` 同步契約零變更、編碼鏈零 diff；純層不碰 DOM/localStorage/API/TTS，60 秒計時歸 translate-tab 管理）
│   ├── phrases-tab.js      ✅ Task2,8  常用句分頁（registerTab('phrases')；Task8 重構為分類 chips 導覽：一次只顯示一類、預設 transport、localStorage `tokyotrip.phrasesCat` 記憶、壞值 fallback PHRASES[0]、空分類 chip 不渲染）
│   ├── import-data.js      ✅ Task3  匯入碼解析/localStorage 存取（App.privateData：get/getRawCode/save/clear/isAvailable）
│   ├── trip-tab.js         ✅ Task3,10  行程分頁（registerTab('trip')，四子區塊 pill 導覽；onShow 冪等；Task10 行程子區塊重構為兩層視圖狀態機：總覽層 .trip-itin-overview（五張精簡日卡，整卡可點）⇄ 單日層 .trip-itin-day（頂部導覽：返回總覽/前後天切換首末日 disabled；時間軸 detail 直接展開非互動）；closure 變數 _itinView 存記憶體禁 localStorage、跨分頁切回保留（B6）；init 今日 isoDate 對上某天直進單日層＋今天 badge，對不上停總覽（舊 B8「範圍外展開 Day1」fallback 廢止）；切換捲動歸零 #tab-trip；四個既有 build 函式（Flights/Hotel/Important/Pills）零變更）
│   ├── map-tab.js          ✅ Task17  地圖分頁（`App.registerTab('map',{onShow})` onShow 冪等（_initialized，後續 onShow 刷新 chip/清單但不重置 A/B closure——G6）；chips 5 顆依天（`Day1 07/21` 格式＋今天 badge）＋日期頭對 `window.TRIP.itinerary` 做**唯讀** isoDate lookup 顯示 day/theme（查不到降級只顯日期不壞頁）；今天判斷自寫 getTodayIsoDate init 算一次跨午夜不刷新（G7），今天在範圍選今天否則第一天；地點清單依當日 MAPDATA 渲染（textContent 防注入）；deep-link 三形態全帶 `hl=zh-TW`——單點 maps/search、路線 maps/dir travelmode=transit（A=目前位置省略 origin／A=地點帶 origin／B=自訂文字 encodeURIComponent），`window.open(url,'_blank','noopener')` 同步手勢棧（iOS 攔彈窗）；可編輯 A/B v1：A 預設目前位置＋還原鈕、B 清單選或自訂輸入（16px iOS 紅線）、A⇄B swap 兩端皆 place 才 enabled；**零 localStorage、不 wrap showTab**（無錄音/TTS/overlay，wrap 鏈維持四層）；MAPDATA 缺載顯示失敗文案不壞頁；DOM/class 見 Task17.api.md）（App.openCouponViewer/closeCouponViewer；O1 additive wrap、O3 z-index 110、pinch/pan/雙擊、壞圖文案）
│   ├── coupons-tab.js      ✅ Task4  折價券分頁（registerTab('coupons',{onShow})；冪等；分類分組渲染；點卡→openCouponViewer）
│   ├── translate-tab.js    ✅ Task5,12,13,15,16  翻譯分頁（registerTab('translate',{onShow}) 冪等；Task12 加「文字/對話」segmented 雙模式（localStorage `tokyotrip.translateMode`，預設與壞值 fallback 皆 'talk'；Task5 文字模式 16 個既有單元零 diff）；文字模式：中⇄日方向切換（`tokyotrip.translateDir` 壞值 fallback zh2ja）、輸入上限 500、防連點、五種錯誤訊息、中→日接 showBigText/speak/複製、日→中僅複製；對話模式（Task15 起面對面雙側版面）：狀態機 idle→recording→recognizing→translating→idle、每側各自麥克風鈕（上半 `.talk-side-ja` 由 CSS rotate 180° 給對面日方、下半 `.talk-side-zh` 正向）、60 秒自動停、`_abortTalk()` 統一清理（timer+abort+cancel+回 idle）、顯示路由＝辨識原文寫說話側 `.talk-side-orig`／譯文寫聽話側 `.talk-side-result`（zh 說→ja 側、ja 說→zh 側；覆蓋式每側只留最近一則，`_side` 記憶體雙槽 {zh:{result},ja:{result,origZh}}；氣泡歷史 `_bubbles`/`_appendBubble`/MAX_BUBBLES/`.talk-history` 已退場，禁 localStorage 不變）、錯誤路由＝API 錯誤→下半 zh（機主處理）／空辨識→說話側／NOT_SUPPORTED→雙 mic disabled、**雙向自動播（zh→ja-JP／ja→zh-TW，G4 守門共用：translate 為當前分頁 && 模式 'talk'——Task13 翻轉 Task12 spec §3「日→中不自動播」；Task15 G4 四行逐字保留，顯示寫入在守門外無條件執行；Task16 起 G4 播音包進 150ms `setTimeout` 延遲守門——`_setSideResult` 立即執行不延遲，G4 於延遲回呼內重驗＋加 `_talkState==='idle'` 守門，防切分頁後到點仍播/延遲窗內搶按 mic 回授）**；Task16 加 `App.speak.unlock()` 唯一呼叫點於 `_onMicClick` idle 路徑（順序＝cancel < unlock < recorder.start，全檔恰一處，禁移到 cancel 之前/禁雙呼叫）、側動作鈕重播/大字（無結果時 disabled 非隱藏；ja 側大字 {ja,zh}、zh 側 {ja,lang:'zh-TW'}——契約沿 Task12.api.md）；wrap App.showTab（三層鏈之中層，見人工補充區）；DOM class 見 Task5.api.md/Task15.api.md（Task12.api.md 氣泡 DOM 節自 Task15 起為歷史存檔））
│   ├── camera-tab.js       ✅ Task6  拍照辨識分頁（registerTab('camera',{onShow})——onShow 冪等層（`_initialized`）＋生命週期層（依視圖＋track 存活分派）；`_startCamera()` getUserMedia environment → `<video playsinline muted autoplay>`＋顯式 play()，失敗→`_activateFallback()` **黏性降級**為原生 `<input capture=environment>`（session 內不重試）；相簿 input（無 capture）無條件並存；`_captureFrame` 一步縮圖（長邊>1600 等比縮、直接目標尺寸 canvas、JPEG q=0.85、拆前綴回 raw base64）；EXIF 走 `<img>`＋objectURL 解碼（禁 createImageBitmap）；`_stopStream()` 四時機停 track（拍照後/wrap 離開分頁/visibilitychange hidden/pagehide，visibilitychange 為獨立 listener 不與 app.js Task14 合併）；流程＝取景/拍照 → `App.api.ocr`（日文 OCR）→ `App.api.translate`（ja→zh-TW）→ 結果卡（原文/譯文/大字 {ja}／{ja,lang:'zh-TW'}/播音 ja-JP/複製/重拍）；OCR 空結果 '' → 回取景＋友善提示、翻譯失敗保留原文＋重試鈕（`_lastOcrText`）；隱私硬約束：影像只存記憶體——零 localStorage、零裸 fetch、base64 只出給 App.api.ocr、objectURL 建撤配對、log 無 base64；wrap App.showTab 第四層（守門：camera 可見 && id≠'camera' → 停 track＋speak.cancel()，call-through）；DOM/class 見 Task6.api.md）
│   └── api.js              ✅ Task5,12,6  Google API 呼叫層（`window.App.api = { ErrorCode, translate, speechToText, ocr }`；POST-only `_postJson`——GET 是破口；金鑰/傳輸/端點三層分離，金鑰層可選鏈讀 APP_CONFIG；ErrorCode 枚舉 NO_KEY/OFFLINE/HTTP_403/HTTP_429/HTTP_OTHER；Task12 端點層追加 `speechToText(base64Audio, languageCode)`（POST speech:recognize，同金鑰同傳輸層；2xx 無 results → resolve ''，「沒聽清楚」非錯誤——與 translate 空結果 reject 語意相反）；Task6 端點層追加 `ocr(imageBase64)`（POST vision images:annotate，`TEXT_DETECTION`＋`languageHints:['ja']`；2xx 內嵌 `responses[0].error` → reject HTTP_OTHER、`fullTextAnnotation.text` → `textAnnotations[0].description` fallback → 皆無 resolve ''——空結果語意比照 speechToText；金鑰/傳輸層零 diff）；不碰 DOM/localStorage/TTS——重用邊界見 Task5.api.md）
├── sw.js                   ✅ Task1–4,8,11,9,10,5,12,13,14,15,6,16,17  Service Worker：cache-first 離線快取（必須在根目錄，scope 才涵蓋全站；現況 CACHE_VERSION='v17'（Task17 bump，與 version.js APP_VERSION 逐字元相等），PRECACHE_URLS 41 筆＝Task2 三新檔＋Task3 兩新檔＋Task4 兩 js＋16 張券圖＋Task5 三筆（config.js/api.js/translate-tab.js）＋Task12 一筆（recorder.js）＋Task14 一筆（version.js）＋Task6 一筆（camera-tab.js）＋Task17 兩筆（mapdata.js/map-tab.js）；Task5 起 A3 config.js network-only 特例整段刪除，method!==GET 直通檢查保留（POST 天然不快取，STT 同受保護）；Task14 檔頭 SOP 註解改寫為兩檔三行紀律，install skipWaiting／activate clients.claim／fetch handler 零 diff）
├── manifest.webmanifest    ✅ Task8  PWA manifest（standalone、portrait、圖示；Task8 background_color/theme_color 改 #F5F6F8，與 index.html theme-color、:root --c-bg 三處一致）
├── icons/                  ✅        icon-192 / icon-512 / apple-touch-icon
├── img/
│   └── coupons/            ✅ Task4  16 張券圖 jpg（全小寫 ASCII 檔名=COUPONS id；總量 4.37MB；與 COUPONS/PRECACHE 三方一致）
├── make_icons.py           ✅        圖示產生腳本（一次性工具，不進快取）
├── make_coupons.py         ✅ Task4  券圖壓縮腳本（一次性工具，不進快取；EXIF 轉正＋白底去 alpha＋長邊≤2000/條碼密集 2600px＋JPEG q=82）
├── make_mapdata.py         ✅ Task17  KML→mapdata.js 轉換腳本（一次性工具，不進快取；資料源 `C:\Olina\其它\東京\2026東京.kml`（repo 外，不進 repo）；6 Folder MMDD 正則→isoDate、同日 0724 兩資料夾依檔內順序合併（築地在前）、KML lon,lat→對調輸出 lat/lon、非 Point/MMDD 解析失敗 fail loud、自檢必印（49 點/5 天/lat∈[35,36]/lon∈[139,141] 越界中止）；行程改期重跑本腳本重生成 mapdata.js）
├── .gitignore              ✅ Task1,5  Task5 起不再排除 js/config.js（金鑰版控化拍板）
├── README.md               ✅ Task1,5  安裝、金鑰設定、部署說明（Task5 金鑰語意改寫：納入版控＋referer/API 兩層限制＋輪替 SOP）
├── DEVELOPMENT_LOG.md      ✅        進度檔（PM 閉環閘依賴）
└── specs/                  ✅        spec 與 sentinel
```

## 分頁（tab）與 Task 對應

| 分頁 | 內容 | 資料來源 | Task |
|------|------|----------|------|
| 常用句 | 分類句庫、點句 → 大字/播音 | `phrases.js` | Task2 |
| 翻譯 | 文字/對話雙模式：文字＝中⇄日輸入翻譯 → 大字/播音；對話＝語音對話即時互譯（錄音→STT→翻譯→**雙向自動 TTS**（iOS 手勢解鎖＋150ms 延遲守門，Task16）→**面對面雙側槽顯示**（上半旋轉 180° 給日方、譯文寫聽話側 ≥28px、覆蓋式每側最近一則）） | Cloud Translation + Cloud Speech-to-Text | Task5 ✅ + Task12 ✅ + Task13 ✅ + Task15 ✅ + Task16 ✅ |
| 拍照 | 取景（getUserMedia 即時預覽，失敗黏性降級原生拍照）/相簿 → Cloud Vision OCR（日文）→ 翻譯（ja→zh-TW）→ 結果卡（大字/播音/複製/重拍） | Cloud Vision + Cloud Translation | Task6 ✅ |
| 行程 | 每日行程、航班、飯店、重要資料 | `tripdata.js` | Task3 |
| 地圖 | 依天地點清單（chips 分 5 天＋TRIP 唯讀 lookup 顯示 day/theme）＋單點開 Google 地圖＋A→B 大眾運輸路線 deep-link（免金鑰、全帶 hl=zh-TW；真導航交 Google 地圖 App） | `mapdata.js`(49 點分 5 天，KML 生成) + `tripdata.js`(TRIP 唯讀) | Task17 ✅ |
| 折價券 | 分類分組券卡 → 點卡開圖片檢視器（pinch 放大給店員掃碼） | `tripdata.js`(COUPONS 16 筆) + `img/coupons/` | Task4 ✅ |

（Task1 只建 shell 與佔位分頁；「大字展示」是共用元件，Task2 首次實作，Task5/6 已重用。Task17 起共六分頁全部落地；Task1 佔位卡已於 Task6/17 全數清除。）

## 依賴關係

- `index.html` → 載入 css、全部 js（config.js 缺檔不得阻斷，onerror 容錯保留）、註冊 sw.js
- `sw.js` → 預快取 app shell 清單（Task5 起**含** config.js；單檔失敗不得使 install 失敗）
- `app.js` → 分頁框架；各功能分頁掛在其上
- `api.js` → 依賴 `window.APP_CONFIG.GOOGLE_API_KEY`；無金鑰時功能頁須顯示設定提示而非壞掉
- 所有路徑一律相對路徑 `./`（GitHub Pages 子路徑部署相容）

## 人工補充區（SA 維護，程式碼掃描抓不到的業務層級依賴）

- **分頁 id 是跨 Task 契約**：Task1 定案的五個分頁 id（見 `Task1.api.md`）被 Task2–6 全部引用；改 id = 全鏈返工。
- **sw.js 版本 bump 依賴**：Task2–6 任何檔案異動要離線生效，都依賴「加預快取清單＋bump `CACHE_VERSION`」SOP；漏做的症狀是「改了沒生效」，迴歸必驗。
- **bump SOP＝兩檔三行（Task14 起永續紀律，取代舊「只改 sw.js 兩常數」）**：任何 bump 必須同步改 `js/version.js` 的 `APP_VERSION`（與 `CACHE_VERSION` **逐字元相等**，QA 每輪機械閘）＋`APP_VERSION_DATE`（`MM/DD`，bump 當天台灣時區）＋`sw.js` 的 `CACHE_VERSION`。版號與日期只存在 version.js 一處（HTML/CSS/其他 js 含註解不得寫死版號字串）；畫面徽章 `vN · MM/DD` 由 app.js 填 `#app-version`。採雙常數而非 importScripts 單一來源＝定案（sw.js 主檔 byte-diff 是最可靠更新訊號，見 Task14.spec.md A1）。Task6 已依此執行（v14→v15，兩檔同步 bump 驗證通過）；之後所有 Task 一體適用。
- **SW 更新機制（Task14 起）**：註冊帶 `updateViaCache:'none'`；visibilitychange(visible)/pageshow → `reg.update()`（promise `.catch` 靜默，不是同步 try/catch——ES5 接不到 rejection）；updatefound＋controllerchange 雙路 → 更新提示 `#update-toast`，`hadController`（register 前快照）守門防首裝誤報（claim 首裝也觸發 controllerchange）；`location.reload` 全 repo 恆恰一處＝toast click handler（無自動 reload，無限重整結構性不存在）。徽章/toast z-index 落 11–99 帶（overlay 100+ 蓋過屬預期）、`#app-version` pointer-events:none、`#update-toast` 需 `[hidden]{display:none}` 防呆。事件時序權威見 `Task14.api.md`。
- **config.js 快取語意（Task5 翻轉，取代舊「雙重排除」）**：config.js 納入版控後即為一般 app shell 檔——**入 PRECACHE、走 cache-first**，金鑰輪替生效路徑＝改內容＋bump CACHE_VERSION＋部署（reload 兩次；過渡期舊金鑰 403 屬預期）。舊 A3 雙重排除（Task1.impact.md）自 Task5 起廢止，權威宣告在 `Task5.api.md`；Task1.api.md 的舊禁令是歷史存檔，不得誤守。index.html onerror 容錯與「APP_CONFIG 未定義合法、可選鏈存取」（A4）繼續有效。
- **Google API 呼叫必須 POST（Task5 起，Task6 繼承）**：sw.js fetch handler 只處理 GET，POST 天然直通不快取、不需為 googleapis 加排除特例；**GET 呼叫是破口**——動態回填段對 200 CORS 回應也 cache.put，金鑰（URL query）進 cache 索引＋翻譯結果被 cache-first 固化。Cloud Vision 亦為 POST，同一約束。機械判準：api.js 對 googleapis 的 fetch 必帶 `method:'POST'`。
- **大字展示元件（Task2 首建）**：依賴 Task1 style.css 的 z-index 層級規範（導覽列低層、≥100 留給 overlay）與 safe-area 變數；Task5/6 重用同一元件。
- **localStorage 命名空間**：`tokyotrip.` 前綴慣例（Task1 起用於 lastTab；Task4 折價券狀態等沿用）。
- **`App.showBigText` / `App.speak` 是跨 Task 契約（Task2 首建）**：簽名以 `Task2.api.md` 定案為準，Task5（翻譯結果）/ Task6（OCR 結果，僅 ja）直接引用，改簽名 = 雙 Task 返工。`showBigText` 僅 `ja` 必填；`speak` 可用性語意 = speechSynthesis 存在（不綁 ja voice）。
- **`App.showTab` 被 bigtext.js 外掛 wrap**（Task2 起）：切分頁自動關 overlay 是 wrap 出來的行為（app.js 本體未改）。之後任何 Task 動到分頁切換行為時，須連 wrap 一起考慮；Task5/6 呼叫 showTab 會連帶關 overlay（文件化於 Task2.api.md）。
- **語音走 iOS 內建 speechSynthesis（已拍板，離線免金鑰）**：不用 Google Cloud TTS，`api.js`（Task5/6）不含 TTS。iOS voices 非同步/GC/cancel-race 等坑的處理集中在 tts.js，Task5 不得繞過 tts.js 自己叫 speechSynthesis。
- **`window.PHRASES` 資料契約（Task2 起被消費）**：phrases-tab.js 唯讀消費 `phrases.js` 的資料結構（分類含 `id`（Task8 起，chips 導覽與 phrasesCat 記憶依賴）+ 句子 ja/zh/romaji 欄位），缺/空時顯示失敗文案不壞頁。改 phrases.js 資料結構或分類 id = 波及 phrases-tab.js 渲染與使用者已存的分類記憶。Task9 已依此紀律完成內容整理（六分類 id 全保留，僅 greetings 的顯示名 cat 改「溝通・語言」）；後續任何句子增/刪/改同樣不得改分類 id。
- **資料隱私分層是 repo 級永續硬約束（Task3 起；Task5 判準修訂）**：repo 內容 = 公開部署內容。公開層（git 追蹤檔全部，含 specs/、DEVELOPMENT_LOG、commit message）禁任何個資真值（護照/保單/訂位代號/姓名/手機）；本機層 = localStorage `tokyotrip.privateData`（匯入碼原字串）。QA 每輪隱私掃描為三段式：工作樹 grep＋**repo 內 `TT1.` 字串 base64 解碼後再 grep**（base64 是純 grep 的盲區）＋git log -p。真值清單由 PM 流程外提供。**Task5 起判準**：API 金鑰（`AIzaSy...`）在 `js/config.js` = 合法（唯一合法位置——已設 referer＋API 限制、可隨時作廢重生；出現在其他 tracked 檔須回報 PM）；個資真值在任何 tracked 檔（含 config.js）= FAIL。
- **tripdata.js 是單檔雙契約**：`window.TRIP`（Task3 消費、schema 見 Task3.api.md）＋ `window.COUPONS`（Task4 消費）同住一檔。任一 Task 動此檔都波及另一 Task；且檔名不變、內容變更也必須 bump CACHE_VERSION 才會生效（cache-first 吃住舊資料，頁面不壞、症狀隱蔽）。
- **匯入碼格式 `TT1.<base64(UTF-8 JSON)>` 是跨界契約（Task3 起）**：權威定義在 `Task3.api.md`——它是 repo 外「電腦端真實匯入碼生成器」唯一能對齊的文件。解析端容忍 URL-safe base64 變體；中文必走 TextEncoder/TextDecoder。改格式 = 已發出的真實匯入碼作廢。
- **localStorage key 登記與清除紀律（Task3 起）**：`tokyotrip.lastTab`（Task1/app.js）、`tokyotrip.privateData`（Task3/import-data.js）；Task4 零新增 key；`tokyotrip.phrasesCat`（Task8/phrases-tab.js，分類記憶，值＝分類 id，壞值 fallback PHRASES[0]，讀寫包 try/catch 私密瀏覽降級）；`tokyotrip.translateDir`（Task5/translate-tab.js，翻譯方向偏好，值 `zh2ja`/`ja2zh`，壞值 fallback `zh2ja`，讀寫包 try/catch）；`tokyotrip.translateMode`（Task12/translate-tab.js 已落地，文字/對話模式記憶，值 `'text'`/`'talk'`，預設與壞值 fallback 皆 `'talk'`，讀寫包 try/catch）。**任何模組的「清除」只准 removeItem 自己的 key，全 repo 禁用 `localStorage.clear()`**——誤用會跨 Task 毀資料。
- **`.tab-section` flex 子項壓縮風險（Task11 SA 發現，永續紀律）**：`.tab-section` 是 `display:flex; flex-direction:column` 的固定高度捲動容器。**任何分頁若在 section 內放多個直接子元素（工具列＋內容區模式），非內容區的子元素必須設 `flex-shrink: 0`**——否則內容高於容器時會被 flex 壓縮（若該子元素帶 overflow-x:auto，自動最小高度歸 0，可被壓到近乎不見；Task11 U2 chips bar 裁切即此因）。修法已落地（Task11 閉環）：`.phrases-chips-bar` 已設 `flex-shrink: 0`。現況唯一多子元素分頁 = 常用句（chips bar＋list area）；trip/coupons 為唯一子元素容器不受影響；Task5/6 新分頁做工具列時同樣適用。
- **主題變數與深色元件的解耦紀律（Task8 起；Task11 閉環後深底清單收斂為僅 `.bigtext-*`/`.cv-*` 兩 overlay——導覽列與航班/飯店卡已轉淺色、回歸全域變數）**：`:root` 主題變數（`--c-bg`/`--c-text`/`--c-text-muted`/`--c-divider`）自 Task8 翻轉為淺色語意；**維持深底的元件（現況僅 `.bigtext-*`、`.cv-*` 兩 overlay）不得引用這些會翻轉的變數**，須用區域性硬編碼色值（合法硬編碼殘留：`#7A8DB8` ×2—bigtext:429 + cv:1534）。後續 Task5/6 新 overlay（深底展示型）同樣適用——引用全域文字變數＝淺色主題下自動壞掉。`--c-primary`/`--c-accent` 為品牌色不翻轉，可安全引用；但 `--c-accent` 當**文字**用在淺底對比僅約 3.5:1，文字場景須用深階 accent 變數 `--c-accent-text`（#2E5BCC，Task8 定案，≥4.5:1）。
- **`App.privateData`（Task3 首建）**：trip 分頁專用（get/save/clear，簽名見 Task3.api.md），非跨 Task 共用元件；命名已占用 `window.App`，後續 Task 避讓。
- **多 overlay 並存紀律（Task4 定案，Task5/6 繼承）**：additive wrap 疊加（每個 overlay 元件各自 wrap `App.showTab`，必須 call-through 前一層）＋同分頁互斥責任在開啟方；z-index 分帶：導覽列 10、bigtext 100、coupon-viewer 110、Task5/6 新 overlay 從 120 起跳。**同一分頁內先後開兩種 overlay**時，開啟方須先關前一個；bigtext.js 無公開 close API，屆時需回報 PM 補簽名。全文見 `Task4.impact.md` O1–O4。（原預警「Task6 相機預覽→OCR 大字最可能踩」經 Task6 SA 分析**解除**：定案設計中取景區是分頁內容非 overlay，camera 分頁唯一 overlay＝重用 bigtext，單 overlay 無互斥，不需 bigtext close API——見 `Task6.impact.md` §2.4。）
- **viewport 沒有 user-scalable=no（易誤設假設）**：index.html viewport 允許頁面縮放。任何自訂手勢 overlay（Task4 檢視器起）須自行抑制頁面縮放（雙指 preventDefault＋gesturestart），**不得改全域 viewport meta**（波及全站已驗收頁面）。
- **PRECACHE 重量前向成本（Task4 起）**：`cache.add` 不走 SW 自身 fetch handler → 每次 bump CACHE_VERSION，18 張券圖（4–7MB）全部重新下載。Task5/6/7 每次 bump 都付此成本（行前家用 wifi 可接受）；若圖量再增長，屆時開 Task 做「install 先從舊快取搬運」優化。
- **precache 失敗的隱蔽性與 QA 冷 install 驗法（Task4 起）**：A4 單檔失敗不炸 install＋動態回填會聯手掩蓋「某張券圖沒進 precache」——線上開過一次頁就回填，離線測試假陽性。QA 離線驗收必須冷 install（清站點資料→重 install 期間不開該分頁→離線→驗全量）。
- **type scale 變數紀律（Task10 落地，永續紀律）**：`:root` 的五階字級變數 `--fs-xl/lg/md/sm/xs`（22/19/17/15/13px，下限 13px）**只授權 `.trip-*` 區塊引用**（Task10 已完成 .trip-* 全數變數化，QA 驗證零硬編碼殘留、零越界）——其他分頁（導覽列/常用句/折價券/overlay）的硬編碼字級是「尚未收斂」的既定狀態，不是破口，不得順手改；未來各分頁字級收斂屬獨立 Task，逐 Task 擴大授權。機械判準：`var(--fs-` 出現在非 `.trip-*` 規則＝越界。F1 三處 Olina 意圖 override（.trip-hotel-name/.trip-important-tel/.trip-flight-no 皆 xl=22px，CSS comment 標注「Olina 意圖：維持醒目」）是合法偏離 spec 表格值的定案，後續 Task 不得「順手歸階」。另 `.trip-import-textarea` 計算字級恆須 ≥16px（iOS 聚焦自動縮放紅線，Task3 起既有約束、Task10 歸階後實測 17px 通過）。
- **`App.registerTab` 只有 onShow、沒有 onHide（Task1 起的結構事實，Task12 SA 確認）**：任何「切走分頁時要做清理」的需求（關 overlay、abort 錄音、cancel TTS）都沒有現成 hook，唯一機制＝additive wrap `App.showTab`（O1 紀律：保原簽名、call-through 前一層）。**wrap 鏈現況已兩層（coupon-viewer.js O1 → bigtext.js B2），Task12 起為三層（外→內，載入順序反向）：coupon-viewer.js（關券檢視器）→ translate-tab.js（Task12 新增：abort 錄音＋cancel TTS，條件＝translate 為當前分頁且目標 id ≠ 'translate'——導覽列重按當前分頁不中斷錄音；cancel 若不加「當前分頁」條件會誤殺常用句播音中的 TTS）→ bigtext.js（關大字 overlay）→ app.js 原函式**。後續 Task 再 wrap 必守 call-through；若要正式加 onHide 屬 Task1 契約異動，須 PM 開 Task。（2026-07-12 SA 修正：本條舊版寫「Task12 起兩層」漏了 coupon-viewer 既有 wrap。）**Task6 起為四層（✅ 已落地）**（camera-tab.js 載入插 translate-tab 後、coupon-viewer 前 → 鏈序外→內：coupon-viewer → camera-tab → translate-tab → bigtext → app.js，QA 已驗 call-through 與載入序）；camera 層守門＝camera 為當前分頁且目標 id ≠ 'camera' → 停 camera track＋cancel TTS——與 translate 層守門條件天然互斥（同時只有一個 section 可見），不互殺；兩個無條件層（coupon-viewer/bigtext）靠關閉冪等自保。詳見 `Task6.impact.md` §2。
- **TTS／bigtext 的 lang 擴充權威 = `Task12.api.md`（Task12 已落地）**：`App.speak(text, lang?)` 選填第二參數預設 'ja-JP'、`showBigText` 選填 `lang` 欄（`ja` 欄語意＝主文字槽），**overlay 為單例，`_lang` 每次呼叫必重設**（殘留會讓後續 ja-only 大字用錯 voice）。Task2.api.md 原簽名仍有效；既有單參數／不帶 lang 呼叫點（phrases-tab、trip-tab、translate-tab 文字模式、bigtext 播音鈕預設路徑）行為零變化。voice 挑選紀律：完整 lang 精確比對優先於前綴（iOS zh-CN/zh-TW 並存，前綴 'zh' 首位無保證）。**歷史語意註記（Task13 起）**：Task12.api.md 語言碼對照表 TTS 欄「`zh-TW`（僅重播鈕）」已成歷史——對話模式自 Task13 起日→中也自動播 zh-TW，行為新權威 = `Task13.spec.md`（Task12.api.md 為閉環存檔不回改，冷 context 讀到該欄不得誤守）。
- **語音辨識走 Google Cloud Speech-to-Text（Task12 拍板翻轉並已落地，不用瀏覽器內建 webkitSpeechRecognition、`js/speech.js` 取消不建）**：錄音／LINEAR16@16k 編碼封裝集中 `js/recorder.js`（比照 tts.js/api.js 紀律：iOS 怪癖不外漏，功能頁不得繞過封裝直接 getUserMedia/AudioContext）；辨識端點 `App.api.speechToText` 集中 api.js 端點層（傳輸層/金鑰層零改動，POST-only 延續）。**兩套語言碼不可混用（硬約束）**：STT languageCode 用 `cmn-Hant-TW`/`ja-JP`，Translation source/target 用 `zh-TW`/`ja`，TTS lang 用 `ja-JP`/`zh-TW`；機械判準＝`cmn-Hant-TW` 只准出現在 speechToText 語境。空結果語意相反：translate 空結果 reject HTTP_OTHER，speechToText 無 results resolve `''`（「沒聽清楚」非錯誤）。金鑰 API 限制現為三個（Translation＋Vision＋Speech-to-Text）。兩個 de-risk 探針頁（`voice-test.html`、`speech-test.html`）已於 Task12 完成時刪除，全 repo 零殘留引用。
- **旋轉容器的 fixed 陷阱（Task15 SA 起，永續紀律）**：對話模式面對面版面（Task15 ✅ 已落地）在 `.talk-side-ja` 打 `transform: rotate(180deg)`——CSS transform 會使該容器成為 `position: fixed` 後代的 containing block，**旋轉容器內禁放 fixed 元素**（會相對容器而非 viewport 定位）。全域 fixed 元件（bigtext/cv overlay、#app-version、#update-toast）皆掛 body 在容器外，不受影響；後續任何 Task 在 talk 側容器內加浮層須改用 absolute 或掛 body。事件座標不受 transform 影響（瀏覽器自動反算），旋轉側內部捲動方向相對對面讀者為自然方向（非 bug）。詳見 `Task15.impact.md` §5。
- **iOS TTS 手勢解鎖與音訊 session 釋放紀律（Task16 SA 起，✅ 已落地——2026-07-12 QA PASS，v16）**：iOS speechSynthesis 首發聲需 user gesture（RC-H）、getUserMedia 錄音後音訊 session 可能滯留 record 類別（RC-I）——兩者是 OS runtime 行為，mock QA 永遠測不到（歷次 QA 只驗「speak 被呼叫」，見 `talk_autoplay.diagnosis.md` 前提 4）。修法契約：`App.speak.unlock()`（tts.js，冪等、發出即解鎖、獨立變數不碰 `_currentUtterance`/`_pendingTimer`）唯一呼叫點在 `_onMicClick` idle 路徑**防回授 cancel 之後**——後續任何 Task 動 `_onMicClick` 不得把 unlock 移到 cancel 之前、不得雙呼叫（組合會互相抵銷，見 `Task16.impact.md` §2）；`recorder.stop()` resolve 語意自 Task16 起＝「音訊 session 釋放（audioCtx.close）完成後」（含逾時保險），依賴 stop 時序的呼叫端不得假設同步釋放；對話自動播的 G4 守門自 Task16 起在 150ms 延遲回呼內評估（含 `_talkState==='idle'` 守門），任何「pending 播音清理」的推理須同時考慮 tts.js 16ms timer 與 translate-tab 延遲 timer 兩層。真發聲驗收永遠歸 Olina 真機（流程外）。
- **MAPDATA↔TRIP 的 isoDate 隱性對齊契約（Task17 SA 起，✅ 已落地——2026-07-12 QA PASS，v17；QA 已驗 5 日 isoDate 兩檔完全一致）**：地圖分頁對 `window.TRIP.itinerary` 做**唯讀** isoDate lookup（顯示 day/theme、預設定位今天），tripdata.js/trip-tab.js 零 diff；但這建立了跨檔資料依賴——**若日後行程改期（TRIP isoDate 變動），`js/mapdata.js` 必須用 `make_mapdata.py` 對新 KML 重新生成**，否則地圖分頁日期頭 lookup 落空（設計上查不到只降級顯示日期、不壞頁，症狀隱蔽）。`mapdata.js` 是生成檔（手改會被覆蓋），資料源 KML 在 repo 外（`C:\Olina\其它\東京\2026東京.kml`，不進 repo）——程式碼掃描永遠看不到這條「repo 外源檔→repo 內生成檔」依賴。座標序陷阱：KML 為 lon,lat，mapdata 為 lat/lon（腳本範圍自檢是機械閘）。map-tab 不 wrap showTab（無需清理資源），wrap 鏈維持四層（QA 已驗四層零 diff）。deep-link 免金鑰（不佔 api.js、不受 referer 限制）；hl=zh-TW 是三形態 URL 的固定參數，後續調整地圖 URL 不得漏帶。
- **iOS localStorage 兩坑（Task3 文案已對應，Task7 真機驗）**：(1) Safari 分頁與加入主畫面的 standalone APP 不共用 localStorage——匯入必須在 APP 內做；(2) iOS 儲存壓力可能清除網站資料，本機層非永久，救濟 = 重貼匯入碼（原文須自行留存）。

## 環境

- 目標裝置：iPhone / Safari（iOS 16+ 假設）、直式
- 本機測試：`python -m http.server 8080`（SW 需 localhost 或 HTTPS）
- 正式：GitHub Pages（Task7）
