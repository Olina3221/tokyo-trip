# tokyo-trip 專案 Spec 索引

> 專案：iPhone 專用東京自由行 PWA（四人同行）。加入主畫面後當 APP 使用。
> 技術棧（已拍板，不重議）：純靜態 HTML + CSS + 原生 JavaScript + Service Worker。**無後端伺服器、無框架、無打包工具。**
> 部署（已拍板）：GitHub Pages。翻譯、OCR 與語音辨識（已拍板）：Google Cloud Translation + Cloud Vision + Cloud Speech-to-Text，同一把 API key，填 `js/config.js`。
> 金鑰版控（2026-07-12 拍板，翻轉舊決策）：**`js/config.js` 納入版控、隨站部署**——金鑰已設「HTTP 參照網址限制＝`olina3221.github.io/*`」＋「API 限制＝Translation＋Vision＋Speech-to-Text 三個」，安全靠限制＋可隨時作廢重生，不再靠「不進 repo」。**個資仍嚴禁進任何 tracked 檔**（隱私分層機制不變）。落地與不變式調和見 `Task5.spec.md`。

## 本 repo 的角色對應（pipeline 用語翻譯）

本專案沒有傳統後端。pipeline 角色對應如下：

- **backend** = 核心邏輯層：`sw.js`、`js/app.js` 的分頁框架邏輯、`js/api.js`、資料檔結構、git/.gitignore/README 等工程面。
- **frontend** = UI 層：`index.html`、`css/style.css`、畫面組裝、iOS 顯示細節（safe-area、大字排版）。
- QA 在 Windows 上以本機 HTTP server + 瀏覽器（含 iPhone viewport 模擬、離線模擬）驗收；**iPhone 真機驗收由 Olina 在流程外做**。

## Task Roadmap（依「佇列順序」欄依序排隊，一次只跑一個；Task 編號永不重用，順序可調）

| 佇列順序 | Task | 標題 | 需網路 | 狀態 |
|---------|------|------|--------|------|
| — | Task1 | PWA 骨架：index.html + 分頁 shell + 離線快取(sw.js) + 加主畫面基礎 + git 初始化 | 否 | ✅ 已完成（2026-07-11 QA PASS，spec: `Task1.spec.md`） |
| — | Task2 | 旅遊常用句 + 大字展示 + 日文語音播放（Web Speech API，離線） | 否 | ✅ 已完成（2026-07-11 QA PASS 45/45，spec: `Task2.spec.md`） |
| — | Task3 | 行程 / 航班 / 飯店 / 重要資料頁（讀 `js/tripdata.js`，離線；個資走本機層匯入碼） | 否 | ✅ 已完成（2026-07-11 QA PASS，失敗 1 次修復後通過；spec: `Task3.spec.md`；真機項移交 Task7） |
| — | Task4 | 折價券專區（讀 `window.COUPONS`，離線；16 張真實券圖，圖片為主體） | 否 | ✅ 已完成（2026-07-11 QA PASS 64/64；收斂為 16 張—移除東京無店的 drugeleven/kintetsu；spec: `Task4.spec.md`；冷 install 真機項移交 Task7） |
| — | Task8 | UX 調整：全 App 改淺色主題 + 常用句分類切換導覽（Olina 實機回饋；B2 已縮小為僅導覽 UI，內容零增刪） | 否 | ✅ 已完成（2026-07-11 QA PASS 靜態驗收全過，phrases.js 機械判準 6+0/41 句零增刪；spec: `Task8.spec.md`；overlay 可讀性/深藍卡對比之真機目視由 Olina 部署後流程外確認） |
| — | Task11 | 底部導覽列淺色化＋字/icon 放大（U1）＋常用句分類 chips 裁切修復（U2）＋航班/飯店卡淺色化（U3）（Olina 第三批實機回饋） | 否 | ✅ 已完成（2026-07-11 QA PASS 靜態 15/15＋整合全過；spec: `Task11.spec.md`；U2 實機視覺由 Olina 部署後流程外確認） |
| — | Task9 | 常用句內容整理：移除 11 句國民基本用語＋新增 9 句實用句（Olina 清單已到、已拍板）；greetings 顯示名改「溝通・語言」（id 不變） | 否 | ✅ 已完成（2026-07-12 QA PASS 靜態驗收全過，39 句（4/9/9/6/5/6）、移除 11/新增 9 逐字一致、六分類 id 零改動、sw.js v8；spec: `Task9.spec.md`） |
| — | Task10 | 行程頁字級統一（五階 type scale）＋單日細節下鑽（總覽⇄單日視圖切換）（Olina 實機回饋）。字級以 Task8 淺色主題為基礎 | 否 | ✅ 已完成（2026-07-12 QA PASS，impact §6 機械判準 1–12 全過＋Playwright 瀏覽器實測；R1 零硬編碼、F1 三處維持 xl=22、R2 狀態機全驗、sw.js v9；spec: `Task10.spec.md`；真機手感由 Olina 部署後流程外確認） |
| — | Task5 | 中 ⇄ 日即時翻譯（Cloud Translation）＋金鑰納入版控（config.js 進 repo）＋翻譯結果接大字展示與語音 | 是 | ✅ 已完成（2026-07-12 QA PASS，金鑰版控化不變式 8 項機械全過＋隱私三段式（翻轉後判準）＋翻譯邏輯靜態驗證全過、sw.js v10；spec: `Task5.spec.md`；真翻譯 E2E 因 referer 鎖 github.io 移交 Olina 部署後 iPhone 流程外驗） |
| — | Task12 | 翻譯分頁對話語音模式：中⇄日語音對話即時互譯（Google Speech-to-Text 辨識＋翻譯＋雙向 TTS＋氣泡歷史） | 是 | ✅ 已完成（2026-07-12 QA PASS，失敗 1 次修復後通過；recorder.js 新檔＋api.js speechToText＋tts/bigtext lang 擴充＋wrap 三層、sw.js v11、兩探針頁已清；spec: `Task12.spec.md`；真對話 E2E 與實機音色由 Olina 部署後流程外驗收） |
| — | Task13 | 對話模式雙向自動播音：日→中辨識翻譯後自動用中文語音（zh-TW）唸出（Olina 實機回饋；翻轉 Task12 spec §3「日→中不自動播」決定） | 是 | ✅ 已完成（2026-07-12 QA PASS，impact §8 機械判準全過；G4 守門共用、中→日零變更、sw.js v12；spec: `Task13.spec.md`；真機 zh-TW 發聲由 Olina 部署後流程外驗收） |
| — | Task14 | 版號可視化＋SW 更新機制可靠化（畫面版號徽章 `vN · MM/DD`＋更新提示一鍵 reload＋reg.update() 檢查時機；Olina 實機回報「刷 10 次拿不到新版、看不出版本」） | 否 | ✅ 已完成（2026-07-12 QA PASS，機械判準 12 項全過 0 FAIL；版號徽章 v13 · 07/12、SW 更新機制（updateViaCache none＋visibilitychange/pageshow 雙觸發＋雙路 toast＋首裝不彈）、location.reload 全 repo 恰一處、sw.js v13＋PRECACHE 38 筆；bump SOP 自此為兩檔三行永續紀律；spec: `Task14.spec.md`；真機更新體感由 Olina 部署後流程外確認） |
| — | Task15 | 對話面對面 UI（翻譯對話模式改為上下切半面對面版面：上半旋轉 180° 給日方、兩側各自麥克風與結果、保留雙向自動播音） | 否 | ✅ 已完成（2026-07-12 QA PASS，0 FAIL；面對面雙側槽＋上半 rotate(180deg)、文字模式零 diff、G4 自動播逐字保留、氣泡歷史退場、sw.js v14 兩檔同步；spec: `Task15.spec.md`；面對面實機手感/真對話 E2E 由 Olina 部署後流程外驗收） |
| — | Task6 | 拍照辨識日文（相機/相簿 → Cloud Vision OCR → 翻譯） | 是 | ✅ 已完成（2026-07-12 QA PASS，0 FAIL；camera-tab.js 取景/黏性降級/縮圖/生命週期＋api.js ocr 端點＋wrap 四層＋sw.js/version.js v15 兩檔同步＋PRECACHE 39＋隱私機械判準全過；spec: `Task6.spec.md`；真機 OCR E2E 由 Olina 部署後流程外驗收） |
| — | Task16 | 對話模式自動播無聲修復（RC-H iOS TTS 手勢解鎖 + RC-I 錄音後音訊 session 釋放；基於已核准之 `talk_autoplay.diagnosis.md`） | 否 | ✅ 已完成（2026-07-12 QA PASS，0 FAIL；speak.unlock 手勢解鎖＋recorder 釋放 Promise 化＋150ms 延遲守門、既有 speak 契約零 diff、sw.js/version.js v16；spec: `Task16.spec.md`；真機兩方向自動發聲由 Olina 部署後流程外驗收） |
| — | Task17 | 地圖功能（混合式）：新增第 6 分頁「地圖」——KML→mapdata.js 依天地點清單＋A→B 一鍵開 Google 地圖大眾運輸路線（純 deep-link 免金鑰） | 否* | ✅ 已完成（2026-07-12 QA PASS，0 FAIL；mapdata 49 點分 5 天、TRIP isoDate lookup 全對齊、deep-link 三形態全帶 hl=zh-TW、placeholder 全清、六分頁、sw.js/version.js v17＋PRECACHE 41；spec: `Task17.spec.md`；*清單離線可看，開路線交 Google 地圖 App；standalone 外跳手感由 Olina 部署後流程外驗） |
| — | Task18 | 文字翻譯「加入常用語」（inline 選六分類）＋自訂句併入所選分類顯示（localStorage `tokyotrip.myPhrases`＋新檔 js/my-phrases.js；原「單一 mine chip」草案已修訂） | 否 | ✅ 已完成（2026-07-13 QA PASS，0 FAIL；my-phrases.js 資料層＋translate-tab additive 白名單 A–E＋phrases-tab onShow 冪等擴充、對話模式全區零 diff、sw.js/version.js v18＋PRECACHE 42；spec: `Task18.spec.md`（修訂版為行為權威）；真機全流程手感由 Olina 部署後流程外驗收、併入 Task7 清單） |
| — | Task19 | 內建常用句也可刪除（刪除＝隱藏：localStorage `tokyotrip.hiddenPhrases` zh+ja 簽名渲染濾除＋內建句列加刪除鈕＋一鍵總還原；phrases.js 零 diff） | 否 | ✅ 已完成（2026-07-13 QA PASS，0 FAIL；T1–T10 功能測試＋spec §E 機械判準全過、對話與文字翻譯全區零 diff、sw.js/version.js v19；spec: `Task19.spec.md`；真機手感由 Olina 部署後流程外驗收、併 Task7 清單） |
| — | Task20 | 重要資料匯入後飯店訂位資訊排版重疊修復（layout fix；Olina 實機回報，與 Task19 一起部署） | 否 | ✅ 已完成（2026-07-13 QA PASS，0 FAIL；`.trip-private-row` 自適應雙模式——短標籤橫排維持/長標籤上下堆疊零重疊、sw.js/version.js v20；spec: `Task20.spec.md`；實機目視由 Olina 部署後流程外確認） |
| — | Task7 | GitHub Pages 部署 + iPhone 真機驗收清單（整併歷次移交的流程外驗收項成行前檢查清單） | — | ✅ 已完成（2026-07-14 PM 直接閉環——文件交付無程式碼變更；交付物 = repo 根目錄 `行前檢查清單.md`：A 部分整併歷次真機驗收項 9 節＋B 部分證件/打包；部署已隨歷次 Task 完成，現況 v20 上線） |
| 備註 | （已立案 → Task17） | ~~KML 地圖功能（行程地點地圖化）~~ | — | Olina 已定案「混合」方向，2026-07-12 立案為 Task17，✅ 已隨 Task17 完成 |
| 備註 | （併入 Task17） | ~~index.html 殘留 Task1 `.placeholder-card` 佔位 HTML 清理~~ | — | ✅ 已隨 Task17 完成（index.html 4 處＋style.css 4 個孤兒選擇器，QA grep=0） |

拆解原則：先做離線可跑的（Task1–4），後做需網路金鑰的難功能（Task5–6），最後部署（Task7）。每個 Task 小到 QA 能單輪 PASS/FAIL。

排序調整（2026-07-11）：Olina 已把 Task1–4 成果部署到 GitHub Pages 並在 iPhone 實測，回饋兩項 UX 問題（深色底戶外難讀、常用句混雜無分類）。**正在用的東西先好用**——Task8 插隊為下一個，翻譯/OCR/部署順延。Task5/6/7 編號不變（既有存檔契約大量指涉）。

排序調整（2026-07-11，第二批實機回饋）：Task10（行程頁字級＋單日下鑽）排 Task8 之後、Task5 之前——同為實機使用回饋，且行程 7/21 開始，行程頁好用優先於翻譯/OCR；Olina 可再調。Task10 與 Task9 檔案零交集（Task10 動 trip-tab.js/style.css，Task9 動 phrases.js），先後皆可，但**兩者都必須在 Task8 之後**（Task8 動 style.css 與 phrases 檔）。

閉環註記（2026-07-11）：**Task8 已正式閉環**，Task9/Task10 的「等 Task8」依賴閘皆已解除；Task9 仍等 Olina 句子清單（Task8 側已無阻擋）。

排序調整（2026-07-11，第三批實機回饋）：**Task11 插隊 Task10 之前**——U2（chips 裁切）是壞掉的既有功能、且 U1/U2 皆為 css 快修，先讓常用句可用；Task10 行程下鑽較大，順延一位。兩任務都改 `css/style.css`，**依序執行**（Task11 先、Task10 後）：`Task10.ready` 已由 PM 收回，Task11 閉環後由 PM 重建。CACHE_VERSION 連動：Task11 預期 v6→v7，Task10 順延為 v7→v8（其 spec 寫「開工時實際值 +1」自動吸收）。

閉環註記（2026-07-11）：**Task11 已正式閉環**（sw.js 現為 v7；深底解耦清單收斂為僅 `.bigtext-*`/`.cv-*`，見 SYSTEM_MAP）。Task10 的「等 Task11 閉環」依賴閘解除，`Task10.ready` 已重建，Task10 為下一個。

排序調整（2026-07-12）：Olina 的常用句清單已到並拍板，**Task9 排 Task10 之前**（Olina 指示「接著做 9、10」）。兩任務檔案零交集（Task9 動 phrases.js，Task10 動 trip-tab.js/style.css），但**都 bump `sw.js` 的 CACHE_VERSION**，故依序執行：Task9 先（v7→v8）、Task10 後（v8→v9，其 spec「開工時實際值 +1」自動吸收）。`Task10.ready` 已由 PM 收回，Task9 閉環後重建。

閉環註記（2026-07-12）：**Task9 已正式閉環**（sw.js 現為 v8；常用句 39 句、greetings 顯示名「溝通・語言」，見 SYSTEM_MAP）。Task10 的「等 Task9 閉環」依賴閘解除，`Task10.ready` 已重建，Task10 為下一個（CACHE_VERSION 開工時 bump v8→v9）。

閉環註記（2026-07-12）：**Task10 已正式閉環**（sw.js 現為 v9；trip 行程子區塊為兩層視圖狀態機、.trip-* 字級全數收斂至五階 type scale 變數、F1 三處重點標題維持 xl=22 為 Olina 意圖定案，見 SYSTEM_MAP）。實機回饋插隊任務（Task8/11/9/10）至此全部清完，佇列回到原 roadmap：**Task5（翻譯）為下一個**，其後 Task6（拍照 OCR）、Task7（部署驗收）。**Task5/6 開工前置：需 Olina 提供 Google Cloud API 金鑰**（Translation + Vision 同一把，填 `js/config.js`，gitignored，不進 repo）——金鑰未到前 PM 不建 `Task5.ready`。

閉環註記（2026-07-12）：**Task5 已正式閉環**（sw.js 現為 v10；翻譯分頁上線——api.js 呼叫層（POST-only 三層）＋translate-tab.js；金鑰版控化落地：config.js 入版控＋進 PRECACHE、sw.js A3 特例廢止、隱私判準翻轉為「config.js 含 API key 合法、個資進任何 tracked 檔仍 FAIL」，見 SYSTEM_MAP）。真翻譯 E2E＋大字/播音實機手感＋離線文案由 Olina 部署後 iPhone 流程外驗收。剩餘佇列：**Task6（拍照 OCR）為下一個**，其後 Task7（部署驗收）。

排序調整（2026-07-12）：新增 **Task12（翻譯對話語音模式）並插隊 Task6 之前**——Olina 實機使用後提出的重要升級（附 Google 翻譯對話模式截圖），對話互譯是本行程翻譯功能的主用途，優先於拍照 OCR。技術前提已 de-risk：`webkitSpeechRecognition` 於 Olina iPhone 主畫面 PWA 模式實測 zh-TW/ja-JP 皆辨識成功（探針 `voice-test.html`，Task12 完成時清除），故不用 Google Speech API；翻譯重用 Task5 `App.api.translate`；TTS 擴充 Task2 `App.speak` 支援 zh-TW（既有 ja 呼叫零變更）。結構定案：併入 translate 分頁做「文字／對話」模式切換，不加第 6 個底部分頁。CACHE_VERSION 連動：Task12 預期 v10→v11，Task6/7 順延（各 spec「開工時實際值 +1」自動吸收）。

決策註記（2026-07-12，Task12 引擎翻轉）：**Task12 語音辨識由「瀏覽器內建 webkitSpeechRecognition」改為 Google Cloud Speech-to-Text API**——Olina 為可靠度拍板（「到國外什麼都不方便、穩一點」）。已 de-risk：探針 `speech-test.html`（錄音→LINEAR16@16k→POST speech:recognize）於 Olina iPhone Safari 與主畫面 PWA 皆辨識成功（`cmn-Hant-TW`/`ja-JP`）；金鑰已加 Speech-to-Text API 限制（現為 Translation＋Vision＋Speech 三個）。`Task12.spec.md` 已重寫為 Google Speech 版；**舊版 SA 產物（`Task12.impact.md`、`Task12.sa_done`）作廢刪除**，`Task12.ready` 重建，SA 重做影響分析。原規劃 `js/speech.js` 取消，改為 `js/recorder.js`（錄音/編碼）＋ `api.js` 端點層加 `speechToText`；載入順序 `api.js → recorder.js → translate-tab.js`；CACHE_VERSION 預期 v10→v11 不變；兩個探針（`voice-test.html`、`speech-test.html`）皆列入 Task12 完成時清理。

閉環註記（2026-07-12）：**Task12 已正式閉環**（QA PASS，累計失敗 1 次——Task5 保護區注解 diff，修復後重測全過；sw.js 現為 v11、PRECACHE 37 筆＋recorder.js；翻譯分頁現為「文字/對話」雙模式，對話模式＝錄音 recorder.js → App.api.speechToText（cmn-Hant-TW/ja-JP）→ translate → 雙語 TTS（App.speak 加 lang 參數）＋氣泡歷史（純記憶體上限 50）；showTab wrap 鏈現為三層 coupon-viewer → translate-tab → bigtext；de-risk 探針 voice-test.html/speech-test.html 已刪，見 SYSTEM_MAP）。真對話 E2E（referer 鎖 github.io，localhost 必 403）與 TTS 實機音色由 Olina 部署後 iPhone 流程外驗收。剩餘佇列：**Task6（拍照 OCR）為下一個**（重用 api.js 端點層追加模式，CACHE_VERSION 開工時實際值 +1＝v11→v12），其後 Task7（部署驗收）。**KML 地圖功能**（Olina 提過的行程地點地圖化構想）待她定案後由 PM 另立 Task，已列 roadmap 備註、不佔當前佇列。

排序調整（2026-07-12）：新增 **Task13（對話模式雙向自動播音）並插隊 Task6 之前**——Olina 實機使用對話模式後回饋：日→中要手動按重播「跟文字版沒兩樣、失去意義」，雙向自動唸才是隨行翻譯官。此為讓正在用的對話模式真正可用的關鍵，優先於拍照 OCR。本任務正式翻轉 Task12 spec「對話流程 §3」的「日→中不自動播」定案（該條當時即預留此調整 Task；Task12.spec.md 為閉環存檔不回改，行為新權威 = `Task13.spec.md`）。純 backend 小改（translate-tab.js G4 區塊＋sw.js bump v11→v12），UI 零變更。Task6/7 順延（各 spec「開工時實際值 +1」自動吸收）。

閉環註記（2026-07-12）：**Task13 已正式閉環**（QA PASS，0 FAIL；sw.js 現為 v12；對話模式現為**雙向自動播音**——G4 守門兩方向共用（translate 當前分頁＋模式 talk），zh→ja-JP／ja→zh-TW，中→日路徑與 Task12 基線逐位元等價；Task12.api.md「zh-TW 僅重播鈕」自此為歷史語意，行為新權威 = Task13.spec.md，已於 SYSTEM_MAP 人工補充區註記——存檔不回改）。真機 zh-TW 發聲／音色由 Olina 部署後 iPhone 流程外驗收。剩餘佇列：**Task6（拍照 OCR）為下一個**（CACHE_VERSION 開工時實際值 +1＝v12→v13），其後 Task7（部署驗收）；KML 地圖待 Olina 定案後另立 Task（roadmap 備註）。

決策註記（2026-07-12）：**Task5 開工前置已解除**——Olina 已申請金鑰、填入 `js/config.js` 並設好網站＋API 限制；同時拍板**金鑰納入版控**（翻轉上一條閉環註記內「gitignored、不進 repo」的舊語意，該句保留為歷史）。因 referer 鎖 `olina3221.github.io`，localhost 實呼叫必 403：QA 只驗邏輯/mock，真翻譯 E2E 歸 Olina 部署後 iPhone 流程外驗。`Task5.ready` 已建。

排序調整（2026-07-12，第四批實機回饋）：新增 **Task14（版號可視化＋SW 更新機制可靠化）並插隊 Task6 之前**——Olina 部署 Task13 後 iPhone 實測：刷新近 10 次拿不到新版、畫面無版號無從判斷更新成敗。此為基礎建設（動 sw.js/app.js，與 Task6 衝突面重疊），先做才能讓後續每次部署的驗收有儀表可看。**重要現況更正**：sw.js 自 Task1 起已有 skipWaiting/claim，「新版 SW 卡 waiting」非根因；真正黏性路徑（更新檢查時機缺失/無版本回饋/重精快取盲刷窗口/HTTP cache）已在 `Task14.spec.md` 逐條盤點並全數覆蓋。定案：版號單一來源 `js/version.js`（`APP_VERSION`/`APP_VERSION_DATE`，徽章顯示 `v13 · 07/12` 於導覽列上方右側）＋與 sw.js `CACHE_VERSION` 雙常數同步（QA 機械判準逐字元相等，自此為 repo 級永續紀律）；更新策略＝`updateViaCache:'none'`＋visibilitychange/pageshow 觸發 `reg.update()`＋「有新版本，點一下更新」提示點擊 reload（**不自動 reload**，無限重整結構性不存在；首裝不彈）；fetch 策略維持 cache-first、**不做 SWR**（評估理由見 spec B3）。連動處置：**`Task6.sa_done` 由 PM 收回**（其 impact 機械基線——sw.js v13、PRECACHE 38 筆、app.js 零 diff——將被 Task14 改掉，留著會讓 backend 誤拾），Task14 閉環後 PM 重建 `Task6.ready` 交 SA 增量複核（`Task6.impact.md` 保留為基底）；Task6/7 各順延一位，版本序自動吸收（Task6 開工時 v13→v14）。

開工註記（2026-07-12）：**Task6 spec 已完成**（`Task6.spec.md`）。設計依 Olina 截圖2 樣態：取景框＋掃描四角＋相簿鈕/大快門鈕；`getUserMedia` 即時取景失敗時**降級為原生拍照 `<input capture>`**（降級內建即為 de-risk，已拍板不另做探針）。OCR 方向固定日→中（語言列靜態標示，反向列 Non-scope）；`App.api.ocr` 端點契約定案 `TEXT_DETECTION`＋`languageHints:['ja']`、空結果 resolve `''`（比照 speechToText）、逐圖 `responses[0].error` reject HTTP_OTHER。camera-tab.js wrap `App.showTab`（離開分頁停 camera track），插 translate-tab.js 後、coupon-viewer.js 前，wrap 鏈成四層。影像只存記憶體不落地（隱私硬約束）。真 OCR E2E 因 referer 鎖 github.io 歸 Olina 部署後流程外驗，QA 驗 mock/降級/靜態判準。`Task6.ready` 已建，待 SA。（後續：SA 已完成影響分析並產 `Task6.impact.md`；Task14 插隊時 `Task6.sa_done` 由 PM 收回，重建時點見佇列表 Task6 列。）

閉環註記（2026-07-12）：**Task14 已正式閉環**（QA PASS，機械判準 12 項全過 0 FAIL；sw.js 現為 v13、PRECACHE 38 筆＋version.js；**bump SOP 自此為「兩檔三行」repo 級永續紀律**——`js/version.js` 的 APP_VERSION（與 CACHE_VERSION 逐字元相等＝QA 每輪機械閘）＋APP_VERSION_DATE＋`sw.js` 的 CACHE_VERSION；畫面右下版號徽章 `v13 · 07/12`＝每次部署的驗收儀表；更新機制＝updateViaCache:'none'＋visibilitychange/pageshow 觸發 reg.update()＋「有新版本，點一下更新」toast（不自動 reload、首裝不彈），見 SYSTEM_MAP 人工補充區兩條新紀律）。真機更新體感（徽章可見、toast 觸發）由 Olina 部署後 iPhone 流程外確認。剩餘佇列（Olina 拍板）：**Task15（對話面對面 UI）為下一個，待 PM 撰 spec**；其後 Task6（拍照 OCR，`Task6.ready` 重建順延至 Task15 閉環後）→ Task7（部署驗收）；KML 地圖待 Olina 定案另立（roadmap 備註）。

開工註記（2026-07-12）：**Task15 spec 已完成**（`Task15.spec.md`）。Olina 實機後拍板：對話模式改為 Google 翻譯式**面對面版面**——螢幕上下切半、上半（日方）整容器 `rotate(180deg)`、兩側各自麥克風鈕/辨識原文/譯文大字/狀態/動作鈕，**雙向自動播音（Task13）完整保留**（G4 守門與 lang 選擇零語意變更，只改文字顯示位置）。PM 定案 v1：譯文顯示在**聽話者那一側**（大字 ≥28px 隔桌可讀）、每側只留最近一則（氣泡歷史 `_bubbles`/`_appendBubble`/`.talk-history` 退場）、API 錯誤一律路由下半（中文，機主處理）、空辨識顯示在說話側（雙語文案）、大字 overlay 不旋轉（「拿起手機展示」定位，bigtext.js 零 diff）、模式切換 segmented 沿用現位不搬家、`.talk-lang-bar` 靜態語言列退場改由每側自帶語言標示。文字模式（Task5）零 diff。對話 DOM 大改，backend 必寫 `Task15.api.md` 供 frontend。bump SOP 兩檔三行：v13→v14。Olina 已聲明截圖僅供概念、部署後實機再微調（微調不阻擋本輪驗收）。`Task15.ready` 已建，待 SA。Task6 的 `Task6.ready` 重建維持在 Task15 閉環後（見佇列表）。

閉環註記（2026-07-12）：**Task15 已正式閉環**（QA PASS，0 FAIL；sw.js/version.js 現為 v14 兩檔同步；對話模式現為**面對面雙側版面**——上半 `.talk-side-ja` 整容器 rotate(180deg)、譯文寫聽話側 ≥28px 隔桌可讀、覆蓋式每側只留最近一則（氣泡歷史退場）、錯誤路由 API→下半 zh／空辨識→說話側、雙向自動播 G4 逐字保留；文字模式 Task5 全區零 diff；「旋轉容器 fixed 陷阱」入 SYSTEM_MAP 人工補充區為永續紀律）。面對面實機手感、旋轉側捲動/大字易用性、真對話 E2E 由 Olina 部署後 iPhone 流程外驗收（微調不阻擋、屬後續 Task）。剩餘佇列：**Task6（拍照 OCR）為下一個**——`Task6.ready` 已重建交 SA 增量複核，開工時 bump v14→v15 依兩檔三行 SOP、PRECACHE 38→39（+camera-tab.js）；其後 Task7（部署驗收）；KML 地圖待 Olina 定案另立（roadmap 備註）。

閉環註記（2026-07-12）：**Task6 已正式閉環**（QA PASS，0 FAIL；sw.js/version.js 現為 v15 兩檔同步、PRECACHE 39 筆；拍照分頁上線——camera-tab.js（getUserMedia 即時取景，失敗**黏性降級**原生 `<input capture>`；相簿並存；一步縮圖長邊 1600；track 停止四時機；影像只存記憶體零落地）＋api.js 端點層追加 `ocr(imageBase64)`（TEXT_DETECTION＋languageHints:['ja']，空結果 resolve ''）；wrap 鏈現為四層 coupon-viewer → camera-tab → translate-tab → bigtext → app.js，見 SYSTEM_MAP）。真機 OCR E2E（referer 鎖 github.io，localhost 必 403）與取景視覺細節由 Olina 部署後 iPhone 流程外驗收。**Olina 原始需求（翻譯/語音對話/大字/常用句/拍照辨識/行程航班飯店/離線/折價券）功能面至此全數閉環**。剩餘佇列：**Task7（GitHub Pages 部署＋iPhone 真機驗收清單）為最後一項**；KML 地圖待 Olina 定案另立（roadmap 備註）。

開工註記（2026-07-12）：**Task16（對話自動播無聲修復）插隊 Task7 之前**——Olina 部署 v15 後 iPhone 實測：面對面對話模式兩方向翻譯文字正常、但都不自動發聲。此為完整性敏感問題（行為沒生效），已走診斷前置階段：Diagnostician 產出 `talk_autoplay.diagnosis.md`（RC-A~G 程式碼側全排除、逐位元 git diff 實證非回歸），Olina 真機確認症狀純落在 speechSynthesis 發聲、兩方向皆無聲（排除 RC-J），核准存活根因＝**RC-H（iOS 首發聲需 user gesture，自動播在 async 回呼被擋）＋ RC-I（getUserMedia 錄音後音訊 session 卡 record 類別）**，兩條併修。修法：`tts.js` 加 `App.speak.unlock()`（手勢內空 utterance 解鎖、冪等、no-op 安全；**正當解除 tts.js 零 diff 保護，只加不改** speak/cancel/pickVoice）＋ `translate-tab.js` `_onMicClick` 開頭呼叫 unlock ＋ `recorder.js` 釋放時序強化（await close／短延遲，backend/SA 定案）。純 backend、零 UI 變更；bump v15→v16 兩檔三行 SOP。QA 只能驗程式面（unlock 冪等/呼叫位置/契約迴歸/版號閘），**真發聲由 Olina 部署後 iPhone 驗，一次沒修好依真機結果迭代**。診斷信號收尾：建 `Task16.ready` 時已刪 `.diagnosed`/`.diagnosis_approved`，`talk_autoplay.diagnosis.md` 保留存檔。Task7 順延為功能修對後的最後收尾。

閉環註記（2026-07-12）：**Task16 已正式閉環**（QA PASS，0 FAIL；sw.js/version.js 現為 v16 兩檔同步、PRECACHE 39 筆零增減；修法落地——`tts.js` 加 `App.speak.unlock()`（RC-H 手勢解鎖：冪等、發出即設旗標、獨立變數不碰既有狀態，additive 四函式本體零 diff）＋`translate-tab.js` unlock 唯一呼叫點於 `_onMicClick` idle 路徑（cancel < unlock < recorder.start）＋ G4 自動播包進 150ms 延遲守門（回呼內重驗 G4＋`_talkState==='idle'`）＋`recorder.js` `_release()` Promise 化、`stop()` resolve 語意改「音訊 session 釋放完成後」（500ms race 保險）；既有 speak 呼叫點與文字模式全零 diff；紀律已入 SYSTEM_MAP 人工補充區並標已落地）。**真機兩方向自動發聲由 Olina 部署 v16 後 iPhone 流程外驗收——一次沒修好依真機結果迭代，另開新 Task 不回改本輪存檔**。剩餘佇列：**Task7（GitHub Pages 部署＋iPhone 真機驗收清單）為最後一項**；KML 地圖待 Olina 定案另立（roadmap 備註）。

開工註記（2026-07-12）：**Task17（地圖功能）立案並插隊 Task7 之前**——Olina 拍板「混合」方向：APP 內依天列地點（綁行程）＋ A→B 一鍵開 Google 地圖算大眾運輸路線；**不做** Directions/Routes API、**不做** APP 內互動地圖（真導航交 Google 地圖 App，Olina 自備離線地圖；deep-link 免金鑰）。資料源＝Olina 的 Google My Maps KML（6 資料夾分天、49 地點；**KML 不進 repo**），新腳本 `make_mapdata.py`（比照 make_coupons.py，一次性工具）轉出 `js/mapdata.js`（依 isoDate 分 5 天、兩個 0724 資料夾合併、**KML 座標序為 經度,緯度 須對調**、lat/lon 範圍自檢）。**PM 定案放新第 6 分頁 `map`（不併行程單日視圖）**——trip-tab.js 894 行狀態機剛閉環不再動刀、新分頁隔離模組 diff 最小；分頁內預設定位今天、日期頭唯讀 lookup TRIP 顯示 day/theme。v1 範圍：任選地點當 A/B（A 預設「目前位置」＝省略 origin 參數）＋自訂文字終點＋A⇄B 互換；**零新 localStorage key**（點庫編輯、A/B 持久化、地點大字皆列 Non-scope）。map-tab.js 不 wrap showTab（四層鏈零變動）；TAB_IDS 加 'map'（additive，既有五 id 零改）；placeholder-card 清理依 roadmap 備註既定決策明列入 spec §D。bump v16→v17 兩檔三行、PRECACHE 39→41。開 Google 地圖的外部跳轉與 standalone 手感由 Olina 部署後流程外驗。`Task17.ready` 已建，待 SA。Task7 順延為最後收尾。

閉環註記（2026-07-12）：**Task17 已正式閉環**（QA PASS，0 FAIL；sw.js/version.js 現為 v17 兩檔同步、PRECACHE 41 筆＋mapdata.js/map-tab.js；第 6 分頁「地圖」上線——`make_mapdata.py`（KML→mapdata.js 一次性工具，KML 不進 repo）＋`js/mapdata.js`（49 點分 5 天生成檔勿手改）＋`js/map-tab.js`（chips 分天＋TRIP 唯讀 isoDate lookup＋A/B 可編輯＋deep-link 三形態全帶 hl=zh-TW＋不 wrap showTab）；TAB_IDS additive 加 'map' 成六分頁；Task1 殘留 placeholder-card 依既定決策全數清除（index.html 4 處＋style.css 4 孤兒選擇器）；MAPDATA↔TRIP isoDate 對齊契約入 SYSTEM_MAP 人工補充區——行程改期須重跑 make_mapdata.py）。standalone 外跳 Google 地圖手感與實地定位由 Olina 部署後流程外驗收。**功能開發至此全部完成，剩餘佇列：Task7（GitHub Pages 部署＋iPhone 真機驗收清單）為唯一剩餘項**——歷次移交 Task7/流程外的真機驗收項（真翻譯 E2E/真對話自動播/真 OCR/地圖 deep-link/面對面手感/更新機制 toast/離線冷 install/localStorage standalone 兩坑）由 PM 於 Task7 spec 整併成行前檢查清單。

開工註記（2026-07-13）：**Task18（文字翻譯「加入常用語」＋自訂句併入所選分類）立案並插隊 Task7 之前**——Olina 需求：文字模式中→日翻完可一鍵存起來，之後在常用句分頁重播給日本人聽（臨時簡單句用這個，要對談才用對話模式）。已拍板：入口只在文字模式中→日結果（日→中 v1 不做）、儲存走 localStorage（靜態 PHRASES 執行期不可改、本輪零 diff）、**加入時手選六分類之一、自訂句併入所選分類的 chip 底下顯示**（本段原「單一 mine chip」草案文字已於閉環時依 `Task18.spec.md` 修訂版更正——該草案於 SA 開工前即由 Olina 拍板取消）。PM 定案：新檔 `js/my-phrases.js` 封裝 `App.myPhrases`（isAvailable/getAll/getByCat/add/remove；兩消費者 translate-tab/phrases-tab 禁繞過直讀 localStorage）、新 key `tokyotrip.myPhrases`（JSON 陣列 `{zh,ja,romaji(恆空),catId,ts}`、新→舊、zh(trim)+ja 雙欄全等去重跨分類亦重複）、`tokyotrip.phrasesCat` 值域**維持既有六分類 id 零變更**；翻譯側加入鈕展開 inline 六分類選擇列（`_lastCatId` session 記憶）；自訂句排該分類最前、帶 `.phrases-item-mine` 標記＋刪除鈕 confirm 防誤觸；**phrases-tab onShow 由純 no-op 擴充為直達重繪當前分類**（跨分頁加句後切回要看得到）；加入鈕回饋比照複製鈕文字暫換（已加入/已在常用語/無法儲存），私密瀏覽優雅降級。bump v17→v18 兩檔三行、PRECACHE 41→42＋載入序 bigtext < my-phrases < phrases-tab。backend＋frontend（輕：加入鈕/刪除鈕/動作列換行樣式），backend 必寫 `Task18.api.md`。真機全流程手感歸 Olina 部署後流程外、整併進 Task7 清單。`Task18.ready` 已建，待 SA。Task7 順延為最後收尾。

閉環註記（2026-07-13）：**Task18 已正式閉環**（QA PASS，0 FAIL；sw.js/version.js 現為 v18 兩檔同步、PRECACHE 42 筆＋my-phrases.js、載入序 bigtext < my-phrases < phrases-tab < translate-tab；文字模式中→日結果加「加入常用語」鈕＋inline 六分類選擇列（去重跨分類、1.5s 文字回饋、收合三路覆蓋）、自訂句經 `App.myPhrases` 併入常用句所選分類最前（`.phrases-item-mine` 標記＋刪除鈕）、phrases-tab onShow 冪等擴充直達重繪、對話模式全區零 diff，見 SYSTEM_MAP）。**行為權威 = `Task18.spec.md` 修訂版**（原「單一 mine chip」草案於 SA 開工前取消；本 INDEX 的佇列表標題與開工註記草案文字已同步更正）。真機全流程手感（翻譯加入→常用句分頁重播）由 Olina 部署後流程外驗收、整併入 Task7 行前檢查清單。剩餘佇列：**Task7（GitHub Pages 部署＋iPhone 真機驗收清單）為唯一剩餘項**。

開工註記（2026-07-13）：**Task19（內建常用句也可刪除）立案並插隊 Task7 之前**——Olina 需求：內建六分類句子現況不能刪（Task18 只有自訂句能刪），要能把已經會的（如「早安」）、不實用的刪掉讓清單清爽。已拍板：內建句在靜態打包 `phrases.js` 執行期不可真刪，**「刪除」＝隱藏**——被刪句以 **zh＋ja 簽名**（內建句無唯一 id，index 會位移不可靠）記入新 localStorage key `tokyotrip.hiddenPhrases`，`phrases-tab.js` 渲染時濾除；內建句列加刪除鈕（沿用 Task18 `.phrases-delete-btn` 樣式與 confirm 文案，使用者不需區分內建/自訂）。PM 定案：**v1 做一鍵總還原**（隱藏清單非空時各分類清單底部渲染 `.phrases-restore-btn`「還原已隱藏的常用句（N 句）」，confirm 後 unhideAll 全部恢復——隱藏為 per-device，無還原等於永久，故必做；逐類還原列 Non-scope）；**封裝擴充 `js/my-phrases.js`**（同屬使用者對常用句的自訂，不開新檔，加 hide/isHidden/getHidden/unhideAll 四方法，unhideAll 只准 removeItem）；無痕降級＝內建句不渲染刪除鈕（與自訂句在無痕下天然不出現對稱，防「點了沒反應」）。本輪**正當解除 Task18 §F5「內建句列表項渲染零 diff」判準**（行為新權威＝Task19.spec.md，Task18 存檔不回改）。translate-tab.js 全檔零 diff、PRECACHE 42 筆零增減、載入序零 diff；bump v18→v19 兩檔三行 SOP。frontend 極輕（僅還原鈕樣式）仍走完整階段。真機手感歸 Olina 部署後流程外、併 Task7 清單。`Task19.ready` 已建，待 SA。Task7 順延為最後收尾。

閉環註記（2026-07-13）：**Task19 已正式閉環**（QA PASS，0 FAIL；sw.js/version.js 現為 v19 兩檔同步、PRECACHE 42 筆零增減；內建句「刪除＝隱藏」上線——my-phrases.js additive 擴充 `HIDDEN_KEY='tokyotrip.hiddenPhrases'`＋hide/isHidden/getHidden/unhideAll 四方法（既有五方法本體零 diff、unhideAll 只准 removeItem）、phrases-tab `_renderListArea` 內建句 zh(trim)+ja 簽名濾除＋每句刪除鈕（沿 Task18 樣式）＋一鍵總還原鈕 `.phrases-restore-btn`（隱藏非空時渲染「還原已隱藏的常用句（N 句）」）、無痕降級雙守衛、translate-tab 全檔與對話模式零 diff；**Task18「內建句列表項渲染零 diff」判準由 Task19 正當解除**，行為新權威＝Task19.spec.md（Task18 存檔不回改）；zh+ja 簽名綁 phrases.js 字面值的跨檔契約已入 SYSTEM_MAP 人工補充區——未來改句子內容會使舊簽名失配、該句復現，屬安全方向）。真機手感（刪除視覺消失、重整持久、還原計數）由 Olina 部署後流程外驗收、併 Task7 清單。**另立案：Olina 回報「重要資料匯入後飯店訂位資訊排版重疊」bug → 新立 Task20（layout fix），與 Task19 一起部署**。剩餘佇列：**Task20（排版修復，待 PM 撰 spec）→ Task7（GitHub Pages 部署＋iPhone 真機驗收清單，最終驗收）**。

開工註記（2026-07-13）：**Task20 spec 已完成**（`Task20.spec.md`）。成因已核實到碼：`.trip-private-row-label` 固定 `width:72px; flex-shrink:0`（style.css L1176）給短標籤設計，訂位小節標籤來自匯入資料（trip-tab.js `b.label`，如「MAPLEHOUSE 淺草 訂單編號」）任意長度 → 溢出重疊。單因單路徑，不走診斷前置。修法定案：**純 CSS 自適應雙模式**——短標籤（護照/保單/公司/緊急電話）維持既有橫排對齊觀感；長標籤自動上下堆疊（label 上可換行、value 全寬在下），零重疊、iPhone 視口無水平溢出、tel: 連結維持 ≥44px。不改資料/匯入碼格式/trip-tab.js 渲染邏輯、**不需重匯**（Olina 現有已匯入資料修好即正常）。分工維持慣例：backend 僅兩檔三行 bump v19→v20（無邏輯變更、免 api.md），frontend 做 CSS。閉環後與 Task19 一次 commit 部署。`Task20.ready` 已建，待 SA。

閉環註記（2026-07-14）：**Task7 已正式閉環——本專案 roadmap 至此全數完成，佇列清空**。Task7 為文件交付（PM 直接產出並閉環，SA/backend/frontend/QA 不跑；無程式碼變更、不 bump 版號、sw.js/PRECACHE 零動）：交付物 = repo 根目錄 **`行前檢查清單.md`**——A 部分整併歷次移交「Olina 部署後 iPhone 流程外驗收」項 9 節（部署/更新 v20＋toast、文字翻譯、對話面對面雙向自動發聲、拍照 OCR、加入常用語＋內建句刪除/還原、行程下鑽/飯店大字/重要資料匯入與排版、折價券 16 張、地圖 deep-link＋Google 地圖 App 繁中、離線可用性＋iOS localStorage 兩坑），B 部分＝證件/手機連線/隨身行李（無個資，證件只勾「確認已帶」）。逐項真機驗收由 Olina 執行；任一項不過不回改已閉環 Task，另開新 Task 迭代。部署本身已於歷次 Task 隨版完成（現況 v20 上線 olina3221.github.io/tokyo-trip），Task7 原標題中「部署」項於此註記核銷。

閉環註記（2026-07-13）：**Task20 已正式閉環**（QA PASS，0 FAIL；sw.js/version.js 現為 v20 兩檔同步、PRECACHE 42 筆零增減；`.trip-private-row` 系列改**自適應雙模式排版**——row 加 flex-wrap＋gap 4px/14px、label 改 width:auto＋min-width:72px＋max-width:100%、value 加 min-width:200px 換行門檻：短標籤（護照/保單/公司/緊急電話）維持 72px 對齊橫排觀感零變化、長標籤（匯入資料任意長度，如「MAPLEHOUSE 淺草 訂單編號」）自動上下堆疊零重疊、390px 視口無水平溢出、tel 連結 ≥44px 維持；不改資料/匯入碼格式/trip-tab.js、Olina 現有已匯入資料免重匯；「標籤寬度是資料驅動的」已入 SYSTEM_MAP 人工補充區——後續動 `.trip-private-*` 排版必須長短兩類標籤同驗，不得回退固定欄寬）。實機目視（長標籤堆疊觀感）由 Olina 部署後流程外驗收。**Task19＋Task20 依既定決策一次 commit 部署**。剩餘佇列：**Task7（GitHub Pages 部署＋iPhone 真機驗收清單）為唯一剩餘項（最終驗收）**。

## 檔案

- `SYSTEM_MAP.md` — 系統地圖（SA 影響分析用）
- `Task1.spec.md` — Task1 規格
- 各 Task 的 `.impact.md` / `.qa_failed.md` 等 sentinel 依 `signal-flow.md` 在本目錄流轉
