# DEVELOPMENT_LOG — tokyo-trip

> 每個 Task 完成時由實作/QA 補完成條目；PM 閉環（刪 `.tested`）前本檔必須有該 Task 條目（進度閘機械檢查）。

## 進度

- 2026-07-11 [PM] 專案啟動：建立 specs/（INDEX、SYSTEM_MAP、Task1.spec）、Task 拆解 7 項、Task1.ready 交 SA。
- 2026-07-11 [SA] Task1 影響範圍分析完成：8 條前向約束（A1–A8）記入 Task1.impact.md，建立 Task1.sa_done。
- 2026-07-11 [Backend] Task1 邏輯層完成：js/app.js（分頁框架 + App.registerTab API + SW 註冊）、sw.js（cache-first + CACHE_VERSION/PRECACHE_URLS 頂部常數 + config.js network-only）、.gitignore、README.md；git init（先 .gitignore 再 add，git ls-files 機械驗證 config.js 未追蹤）；Task1.api.md 輸出五分頁 id 定案、腳本載入順序規則、APP_CONFIG 缺席契約、localStorage 前綴、sw.js SOP。
- 2026-07-11 [Frontend] Task1 UI 層完成：index.html（iOS meta 含 viewport-fit=cover / apple-mobile-web-app-capable / black-translucent / apple-touch-icon / manifest / theme-color；五分頁容器 id="tab-{id}"；底部導覽列 data-tab="{id}"；腳本載入順序 A6 定死；所有引用 ./ 相對路徑 A5）、css/style.css（深色主題 #1F2A5C；env(safe-area-inset-*) 四邊處理；#nav-bar z-index:10 A7；overlay ≥100 預留註解；觸控目標 min-height 56px ≥44px；佔位卡可整塊替換結構 A1）。
- 2026-07-11 [QA] Task1 驗收 PASS：HTTP 200 全檔、五分頁容器/導覽契約、SW cache-first + config.js 排除 + 單檔容錯、相對路徑全清、iOS meta 契約、git 安全性（config.js 未追蹤）逐項通過；證據存 Task1.tested（PM 閉環後以本條目為紀錄）。
- 2026-07-11 [PM] Task1 閉環：刪 Task1.tested、確認無 sentinel 殘留（保留 Task1.impact.md / Task1.api.md 存檔）、SYSTEM_MAP 狀態更新為已完成、INDEX 標 Task1 完成、Task2 為下一個。
- 2026-07-11 [Backend] Task2 邏輯層完成：js/tts.js（App.speak + isAvailable + cancel；cancel-then-speak 契約、16ms iOS 延遲、GC 保留參照、voiceschanged 冪等、降級靜默）、js/bigtext.js（App.showBigText + wrap App.showTab；B1 掛 body、B2 外掛 wrap、B3 ja-only sub 不渲染、B5 無 history、B6 關閉 cancel）、js/phrases-tab.js（App.registerTab('phrases')；冪等渲染、點句→showBigText、點喇叭→speak、PHRASES 缺/空顯失敗文案）；sw.js bump v1→v2、PRECACHE_URLS 加 tts/bigtext/phrases-tab 三支；Task2.api.md 記錄所有簽名、DOM class 名、showTab wrap 契約、Frontend script 順序、Task5/6 呼叫範例、B5/B6 定案。
- 2026-07-11 [Frontend] Task2 UI 層完成：index.html 依 api.md 在 app.js 之後插入三支 script 標籤（tts.js → bigtext.js → phrases-tab.js，順序定死，保留 Task5-6 插入佔位註解）；css/style.css 新增常用句分頁樣式（.phrases-container/.phrases-group/.phrases-cat sticky 分類頭/.phrases-list/.phrases-item/.phrases-item-body 觸控≥64px/.phrases-zh 17px 主文/.phrases-ja 14px/.phrases-romaji 11px/.phrases-speak-btn min-width 56px/.phrases-error 失敗文案）與大字 overlay 樣式（.bigtext-overlay z-index:100 fixed 全螢幕深色底 + safe-area 四邊 padding/.bigtext-ja clamp(48px,13vw,96px) 超大白字 + overflow-wrap break-word/.bigtext-sub ja-only 由 JS 控制隱藏/.bigtext-zh 20px/.bigtext-romaji 14px/.bigtext-speak-btn/.bigtext-close-btn 各 min-height 56px ≥44px）；touchmove 穿透由 bigtext.js 的 preventDefault 攔截（B6/規則 7），CSS 側無需額外處理。
- 2026-07-11 [QA] Task2 驗收 PASS（45/45，0 FAIL）：迴歸冒煙（Task1 骨架 A2/A3/A5/A6、app.js 未改）、本次 spec 冒煙（sw.js v1→v2 bump、三新模組入 PRECACHE_URLS）、整合測試 B1–B7 全過（overlay 掛 body、showTab wrap、ja-only 不渲染 sub、speak 語意契約含 cancel-then-speak 16ms、無 pushState、關 overlay cancel 語音、sw.js 只動兩常數）、A7 z-index/觸控目標、phrases 分頁冪等渲染與失敗文案、Non-scope 無越界、金鑰安全（config.js 未追蹤）；證據存 Task2.tested（PM 閉環後以本條目為紀錄）。
- 2026-07-11 [PM] Task2 閉環：刪 Task2.tested、確認無暫時性 sentinel 殘留（保留 Task2.impact.md / Task2.api.md 存檔）、SYSTEM_MAP 更新（tts/bigtext/phrases-tab 標已完成、sw.js 註記 v2）、INDEX 標 Task2 完成、Task3 為下一個。
- 2026-07-11 [Backend] Task3 邏輯層完成：js/tripdata.js（window.TRIP 整檔替換為真實行程／航班 MM626/MM625／飯店 MAPLEHOUSE 浅草真實資料，schema 升級為 itinerary items `{time,title,detail}` 物件化、新增 isoDate 欄位、移除 members；window.COUPONS 原樣保留；檔頭加隱私警告）；js/import-data.js（App.privateData 介面：get/getRawCode/save/clear/isAvailable；匯入碼格式 TT1.<base64(UTF-8 JSON)>、去全 whitespace、容忍 URL-safe 變體 B1、TextDecoder UTF-8 中文解碼、解析成功才寫入 B6-fail-safe、clear 只用 removeItem 單一 key B2）；js/trip-tab.js（App.registerTab('trip',{onShow})；四子區塊 pill 導覽；行程日卡展開/收合 + B8 預設定位當日；航班卡；飯店卡含複製/開地圖/App.showBigText ja-only；重要資料公開段 tel: 連結 + 本機私人段 import/export/clear；B6 onShow 冪等僅首次建 DOM，匯入成功/清除後僅重繪私人段）；sw.js bump v2→v3、PRECACHE_URLS 加 import-data.js/trip-tab.js；隱私掃描：全 repo grep 已執行零個資命中（members 陣列移除、hotel note 無訂房姓名、important 無等待手填佔位列）；Task3.api.md 輸出（TRIP 最終 schema、匯入碼格式權威定義＋生成器須知、App.privateData 簽名、DOM class 名、onShow 策略、假資料測試碼＋解碼 JSON、localStorage key 登記）。
- 2026-07-11 [Frontend] Task3 UI 層完成：index.html 在 phrases-tab.js 之後插入兩支 script（import-data.js → trip-tab.js，順序定死，含 Task3 comment 與 Task5-6 佔位 comment）；css/style.css 新增 Task3 trip 分頁完整樣式（共 8 個視覺區塊）：Pill 導覽（position:sticky top:0、overflow-x auto 橫捲、44px 觸控目標、active 狀態 accent 背景）、行程日卡（trip-day-header min-height 56px、展開時 accent 背景淡底、trip-day-body/item-header/item-detail 含 white-space:pre-wrap 換行支援、trip-item-time tabular-nums 對齊）、航班卡（trip-flight-card primary 深藍背景圓角卡、route 區左右去回程 flex 並排、trip-flight-no 24px 大班號）、飯店卡（trip-hotel-name 26px、address-ja word-break:break-all 避免地址橫溢、tel 連結 min-height 44px、三大按鈕 trip-hotel-actions flex-wrap）、重要資料公開段（tel 連結 22px font-weight:700 大字可點）、本機私人段（trip-private-unavail 黃色警示底；trip-private-info 含 standalone 與留存提示；trip-btn-import 全寬 56px 大按鈕）、匯入區（trip-import-textarea border 2px accent 引導貼上、font-size 16px 防 iOS 縮放、trip-btn-confirm 全寬 54px）、已匯入欄位列（trip-private-row 含 tel 連結 44px 觸控目標）、操作列（reimport/export/clear 三按鈕，clear 紅色警示色）、匯出區（trip-export-textarea monospace 唯讀）、暫時提示 trip-tip（animation trip-tip-fade）；B7 長地址：.trip-hotel-address-ja word-break:break-all 確保 section 內不溢出、bigtext overlay 沿用既有 .bigtext-ja overflow-wrap:break-word 不需額外 CSS；padding-bottom:32px 防 iOS 鍵盤遮住底部。
- 2026-07-11 [Backend] Task3 修復（QA 退回，失敗 1 次）：js/trip-tab.js `_renderPrivateEmpty()` 第 421 行 `_buildImportArea(container, importBtn, false)` 改為 `true`；空狀態匯入 textarea 現預設隱藏，點「匯入」才展開，與 spec 七及按鈕文案一致。對外介面（App.privateData 簽名、DOM class、api.md）未動，純內部邏輯修復。
- 2026-07-11 [QA] Task3 驗收 PASS（累計失敗 1 次後修復重測通過）：迴歸冒煙（Task1/2 功能全過、sw.js v3 + PRECACHE 含 import-data/trip-tab、app.js 未改、COUPONS 保留）、隱私三段式掃描零命中（工作樹 grep＋TT1. 碼 base64 解碼後 grep＋git log -p，唯一實碼為全假測試值）、本次 spec 冒煙（App.privateData 介面完整、TRIP schema 四大欄位、5 天 isoDate、parseImportCode 6 案例）、整合測試（匯入碼 B1 含 URL-safe/中文/fail-safe、航班 MM626/MM625 真值、飯店三鈕、B2 removeItem 紀律、B6 onShow 冪等、修復重點：空狀態匯入框預設隱藏驗證通過）、Non-scope 無越界；真機項目移交 Task7；證據存 Task3.tested（PM 閉環後以本條目為紀錄）。
- 2026-07-11 [PM] Task3 閉環：刪 Task3.tested、確認無暫時性 sentinel 殘留（保留 Task3.impact.md / Task3.api.md / Task3.spec.md 存檔）、SYSTEM_MAP 更新（tripdata.js/import-data.js/trip-tab.js 標已完成、sw.js 註記 v3、index.html/style.css 註記 Task3）、INDEX 標 Task3 完成、Task4（折價券）為下一個。
